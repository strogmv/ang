"""FastAPI app scaffold generated by ANG."""

from fastapi import FastAPI
from app.routers import (
    report,
)

app = FastAPI(title="PDF Chart Service", version="0.1.0")
app.include_router(report.router)
