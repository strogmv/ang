# ANG: план работ по безопасности сборки и удобству для агентов

Документ написан для исполняющей модели. Он самодостаточен: все пути, команды и
номера строк проверены 2026-09-14 на коммите `050e46d0` репозитория ANG.
Читай его целиком до начала работы. Каждый пункт содержит: проблему с
доказательствами, требуемое поведение, шаги, критерий приёмки и запреты.

Порядок выполнения строго P0 → P1 → P2 → P3. P4 — по желанию, после всего.

---

## 0. Контекст, который нельзя выяснить из кода

### 0.1 Что такое ANG и где что лежит

- Репозиторий генератора: `~/Desktop/work/ang`. Go-модуль. CLI — `cmd/ang/*.go`,
  компилятор — `compiler/`, шаблоны — `templates/`. 241 файл `_test.go`,
  фикстуры в `cmd/ang/testdata`.
- Эталонный проект-потребитель: `~/Desktop/work/deal/dealingi-back`. Это **единственный
  боевой проект** на ANG; любое изменение генератора проверяется на нём.
- Проект пиннит ревизию генератора файлом `ang.lock`:
  ```
  repository=https://github.com/strogmv/ang.git
  commit=050e46d0fe4536bb10be54e33604e449012ce642
  ```
  `make generate` в проекте (Makefile, цели `verify-ang` → `build-ang` → `generate`)
  проверяет, что checkout `../../ang` стоит ровно на этом коммите и чист, собирает
  `bin/ang` и запускает `bin/ang build`. `make sdk` делает то же с флагом
  `--frontend-app-dir ../dealingi-front/src/@sdk`.
- CI проекта (`.github/workflows/ci.yml`, джоба `generated-output`) клонирует ANG на
  коммит из `ang.lock`, запускает генерацию и требует `git diff --exit-code` и
  отсутствие untracked-файлов. **На коммите `050e46d0` эта джоба зелёная.** Это
  базовая линия: после твоих изменений она обязана остаться зелёной, пока
  `ang.lock` не обновят намеренно.

### 0.2 Как устроена сборка сейчас (`cmd/ang/build_transaction.go`)

Прочитай этот файл целиком перед P0. Кратко:

1. `generatedTransactionPaths()` (строка 451) — **фиксированный список каталогов**,
   которыми «владеет» транзакция: `cmd/server`, `internal`, `api`, `db`, `deploy`,
   `sdk`, `scripts`, `tests`, `.ang/cache`, `.env.example`, `ang-manifest.json`,
   `atlas.hcl`, `sqlc.yaml` плюс backend/frontend-каталоги целей.
2. `beginBuildTransaction()` (31) — для каждого такого пути делает снимок в
   `.ang-build-transaction-*/stage`.
3. `CreateWorkspace()` (124) — создаёт `.ang-build-workspace-*/project`, где всё
   верхнеуровневое — символические ссылки на настоящий проект, а «принадлежащие»
   пути материализованы копией.
4. Генерация и `go build` идут в workspace.
5. `CaptureWorkspace()` (240) — копирует принадлежащие пути из workspace в `stage`.
6. `Commit()` (62) — для каждого пути: `os.Rename(path → backup)`, затем
   `os.Rename(stage → path)`.

Следствие, которое и есть главная проблема: **каталог заменяется целиком снимком,
сделанным в начале сборки.** Всё, что человек или агент записал в `internal/`,
`db/`, `deploy/`, `tests/`, `scripts/` за время сборки (минуты), исчезает при
`Commit`. Это не «копирование проекта», это подмена каталогов по гранулярности
«каталог», а не «файл».

Насколько это широко в эталонном проекте (посчитано `git ls-files`):

| каталог | файлов | с маркером генерации (`.gen.go`, `/gen/`) |
|---|---|---|
| `internal` | 1652 | 865 |
| `db` | 167 | 0 |
| `deploy` | 86 | 0 |
| `tests` | 214 | 0 |
| `scripts` | 12 | 0 |

`db/migrations/*.sql`, `deploy/prod/*.yaml`, `tests/e2e/*.test.ts` — ручные файлы,
и они живут внутри каталогов, которые сборка подменяет целиком. Отсутствие маркера
не значит «ручной» (например, `internal/transport/http/common.go` генерируется без
суффикса), поэтому **владение нужно вести по списку файлов, а не по каталогам**.

### 0.3 Копирование SDK во фронт (`cmd/ang/build_helpers.go:368`)

`copyFrontendSDK(srcDir, appDir)`:
- вызывает `ensureSDKTarget(appDir)` (`cmd/ang/sdk_target_guard.go`) — отказывается,
  если в каталоге есть маркеры проекта (`.git`, `package.json`, `src`,
  `node_modules`, `go.mod`, `cue`) или он не пуст и без `sdk-manifest.json`;
- затем `os.RemoveAll(appDir)` и запись файлов по одному.

Защита появилась после того, как флаг, направленный на корень фронта, трижды удалял
`src/.git`. Защита хорошая, оставшаяся дыра — **удалить-потом-записать**: если
запись упадёт посередине, SDK во фронте нет, а откатывать нечего.

### 0.4 Что уже есть и что не надо изобретать заново

- `ang build --dry-run` (`cmd/ang/dry_run.go`, вызовы в `build.go:120–134`) —
  генерирует во временный корень и пишет манифест: для каждого файла
  `create | update | unchanged`. **Содержимого изменений в манифесте нет.**
  MCP-инструмент `ang_dry_run` (`internal/mcp/tools_core.go:189`) возвращает этот
  манифест.
- `ang build --target <name|lang|stack>` (`build_helpers.go:60`) — сборка только
  выбранных целей. Это гранулярность «цель», не «сервис».
- `ang vet logic` — «Audit embedded Go snippets for syntax errors» (`main.go:149`,
  диспетчер `case "vet"` → `runVet`, `main.go:64`). Парсит синтаксис. **`go/types`
  в репозитории не используется нигде** — проверки типов до полной сборки нет.
- `ang fmt` — канонический формат CUE и нормализация алиасов flow.
- `cmd/ang/template_hash.go`, `.ang/cache` — хеширование шаблонов и кэш; смотри,
  прежде чем делать инкрементальную сборку (P2.2).
- `cmd/ang/artifact_manifest.go` — запись артефактов сборки. Если сборка уже
  фиксирует каждый созданный файл, **это источник правды для владения в P0.1**;
  если нет — сделай его таким.
- Выход форматируется: `compiler/templateset/emit.go:96` — `imports.Process`,
  `compiler/emitter/*.go` — `format.Source`. Значит, беспорядок из п. 3 живёт только
  в CUE-исходниках, не в результате.

### 0.5 Масштаб Go-кода внутри CUE (эталонный проект)

- 162 файла `cue/api/impl_*.cue` содержат **27 823 строки Go** внутри
  строк `"""…"""` (ключ `code`, определения `#…Func`, поля `expr`, `value`).
- Самые длинные строки: `impl_b2b_negotiation_search.cue:135` — 2 883 символа,
  `impl_company_products_import.cue:786` — 2 789, `impl_b2b_share_links.cue:137` — 2 384.
- Частота действий flow: `mapping.Assign` 1561, `logic.Check` 660, `logic.Call` 627,
  `repo.Find` 367, `flow.If` 347, `policy.Check` 233, `math.Expr` 193.

Что это значит для агента: он пишет Go без gofmt, без компилятора и без
подсказок, а узнаёт об ошибке после полной сборки на минуты — и сборка при этом
может подменить его же незакоммиченные файлы (п. 0.2). Из-за этого агент
**обходит генератор**: правит тест вместо CUE, копит правки, боится запускать.

---

## P0. Сборка не трогает ничего, кроме своих файлов

### P0.1 Владение по списку файлов, не по каталогам

> **Реализовано 2026-09-14, ветка `agent-ergonomics-p0` — другим способом, чем
> описано ниже.** Список эмитированных файлов собрать надёжно нельзя: записи
> разбросаны по 49 вызовам в 30 файлах, центрального писателя нет, и любой
> пропущенный путь стал бы новой дырой. Вместо этого `Commit()` делает
> **трёхстороннее слияние по файлам** (`cmd/ang/build_transaction.go`, комментарий
> к `buildTransactionEntry`):
>
> - `base` — снимок пути на старте транзакции, `stage` — результат генерации,
>   `path` — проект в момент коммита;
> - файл, который генератор не менял (`stage == base`), не трогается вообще —
>   любые правки, созданные и удалённые во время сборки файлы сохраняются;
> - изменённый генератором файл публикуется rename-ом; если его же изменили в
>   проекте, версия проекта сначала копируется в `.ang/conflicts/<время>/`, путь
>   печатается в предупреждении;
> - удаляются только файлы, удалённые генератором и не тронутые в проекте; пустые
>   каталоги убираются, только если существовали на старте и их нет в `stage`;
> - откат при ошибке — по журналу выполненных rename.
>
> `stage` для путей вне рабочей копии заполняется лениво, поэтому копирований не
> больше, чем раньше, а при коммите трогаются только реально изменённые файлы.
> Секция `generated_files` в `ang-manifest.json` **не нужна и не добавлена**;
> выход генератора не изменился. Удаление устаревших сгенерированных файлов
> работает ровно как до изменения (раньше это делала подмена каталога снимком,
> теперь — слияние), новой логики очистки не появилось.
>
> Тесты: `TestBuildTransactionCommit*`, `TestBuildTransactionEntryNeverStagedIsLeftAlone`,
> интеграционный `TestRunBuildKeepsFilesWrittenDuringBuild` (через тестовую точку
> `buildBeforeCommitHook`). Описание ниже оставлено как исходная постановка.

**Проблема.** См. 0.2. `Commit()` подменяет `db/`, `deploy/`, `tests/`, `scripts/`,
`internal/` целиком.

**Требуемое поведение.**
- Сборка знает точный список файлов, которые она создала в прошлый раз, и точный
  список, который создаёт сейчас. Она пишет **только** эти файлы. Удаляет только те,
  что были в прошлом списке и отсутствуют в новом.
- Любой файл в «принадлежащем» каталоге, которого нет в списке, сборка не читает,
  не копирует, не удаляет и не переименовывает. Ручной `db/migrations/x.sql`,
  записанный во время сборки, после `Commit` на месте, байт в байт.
- Список хранится в проекте в детерминированном виде (отсортирован, относительные
  пути с `/`), например в `ang-manifest.json` — если `artifact_manifest.go` уже
  пишет туда список файлов, расширь его; если нет, добавь секцию
  `"generated_files": [...]`. Файл сам входит в список.
- Первый запуск на проекте без списка: сборка должна работать как раньше **один
  раз**, чтобы создать список, и печатать предупреждение. Не ломай миграцию
  существующего проекта.

**Шаги.**
1. Найди, где известен набор эмитируемых файлов (эмиттеры в `compiler/`,
   `templateset/emit.go`; собери путь каждого записанного файла в один срез).
2. Замени модель транзакции: вместо `entries` по каталогам — `entries` по файлам.
   `Commit` делает `rename(stage/file → file)` для каждого файла; удаление — для
   разности старого и нового списка. Родительские каталоги создаются, пустые
   каталоги после удаления убираются, непустые — нет.
3. `CreateWorkspace` остаётся overlay-ом на символических ссылках, но
   материализует только файлы из объединения старого и нового списка.
4. Сохрани атомарность на уровне файла (rename), а откат — по журналу выполненных
   rename, как сейчас в `restoreCommitted`.

**Приёмка.**
- Тест в `cmd/ang`: создать проект-фикстуру, запустить сборку, во время сборки
  (через хук/канал в тесте) записать `db/migrations/manual.sql`,
  `tests/e2e/manual.test.ts`, `internal/service/manual_note.go`; после сборки все
  три на месте с исходным содержимым.
- Тест: удалить из CUE один эндпоинт, собрать — файлы, которые перестали
  эмитироваться, удалены; никакие другие файлы не тронуты (сравнение дерева до/после
  по списку).
- В `dealingi-back` на ветке с твоими изменениями: `make generate` → `git status`
  показывает изменения **только** в `ang-manifest.json` (новая секция). Затем
  `make generate` второй раз → `git status` чист.

**Запреты.** Не меняй список каталогов в `generatedTransactionPaths` «на глаз» —
замени сам принцип. Не добавляй эвристику по суффиксу `.gen.go`: см. 0.2, она
неверна.

### P0.2 Копирование SDK во фронт — атомарно

> **Закрыто P0.1, отдельного кода не потребовалось (2026-09-14).**
> `copyFrontendSDK` вызывается только шагом сборки (`step_registry.go`), а
> `build.go` заранее подменяет `--frontend-app-dir` на stage транзакции
> (`targetOutput.FrontendAppDir = stagePath(...)`). `RemoveAll` и запись идут в
> stage; реальный каталог SDK меняется только в `Commit` — пофайловым слиянием с
> откатом. Копирование, упавшее посередине, роняет сборку, транзакция
> откатывается, SDK приложения не тронут. Доказано тестами
> `TestFrontendSDKCopyFailureDuringBuildLeavesAppSDKUntouched` и
> `TestFrontendSDKCopyPublishesThroughMerge`. Если `copyFrontendSDK` когда-нибудь
> позовут вне сборки, этот пункт снова актуален.

**Проблема.** См. 0.3: `RemoveAll` перед записью.

**Требуемое поведение.**
- SDK собирается в соседний временный каталог `<appDir>.ang-stage-*` на той же
  файловой системе, затем `rename(appDir → appDir.ang-backup-*)`,
  `rename(stage → appDir)`, удаление backup. При ошибке — обратный rename.
- `sdk-manifest.json` пишется **всегда** и первым, чтобы `ensureSDKTarget`
  в следующий раз имел положительное доказательство.
- `ensureSDKTarget` не трогать; его тесты (`sdk_target_guard_test.go`) должны
  остаться зелёными.

**Приёмка.** Тест: сымитировать ошибку записи на середине копирования (например,
файл с недопустимым именем во входном наборе) — целевой каталог остался в прежнем
состоянии, backup удалён, ошибка возвращена.

### P0.3 Бинарник сам проверяет `ang.lock`

> **Реализовано 2026-09-14** (`cmd/ang/ang_lock.go`). ldflags не понадобились: Go
> сам вшивает `vcs.revision` и `vcs.modified` (`debug.ReadBuildInfo`). `ang build`
> — во всех фазах, включая `plan`/`apply` и dry-run — сверяет ревизию с `commit=`
> из `ang.lock` до генерации. Расхождение — `ANG_LOCK_MISMATCH` (`docs/error_codes.md`,
> подсказка в `ang doctor`), обход — `--allow-lock-mismatch` (пробрасывается и в
> dry-run-подпроцесс `template diff`). Нет VCS-информации или бинарник собран с
> незакоммиченными правками — только предупреждение в stderr. `ang version`
> печатает ревизию.
>
> Проверено на dealingi-back: бинарник `f2194fae` без флага отказал с кодом 1, не
> начав генерацию; с флагом dry-run прошёл — **2094 файла, 0 новых, 0 изменённых**.
>
> **Ограничение:** проверка есть только в бинарниках, собранных после этого
> изменения. На 2026-09-14 `dealingi-back/bin/ang` собран из `0bb3b928`, а
> `~/go/bin/ang` — из `09a4efaf` с грязным деревом; оба старше пина и проверку не
> содержат. Пока их не пересобрать, прямой запуск по-прежнему опасен.

**Проблема.** Проверку ревизии делает Makefile проекта (`verify-ang`). Запуск
`ang build` напрямую, из другого каталога или глобального `~/go/bin/ang`, эту
проверку обходит — и генерирует другой контракт.

**Требуемое поведение.**
- Коммит и «грязность» дерева вшиваются в бинарник через `-ldflags -X` при
  сборке (`Makefile` в ANG и `build-ang` в проекте передают
  `git rev-parse HEAD` и `git status --porcelain`).
- `ang build` при наличии `ang.lock` в корне проекта сравнивает `commit=` с
  вшитым. Не совпало — ошибка с кодом диагностики и текстом «expected X, running Y,
  run: git -C <ANG_SRC> checkout X»; собрать можно только с явным
  `--allow-lock-mismatch`. Бинарник без вшитого коммита (сборка `go run`) — только
  предупреждение.
- `ang --version` печатает коммит.

**Приёмка.** Тест на функцию сравнения; ручная проверка: в `dealingi-back`
запустить `~/go/bin/ang build --dry-run` (старый бинарник от 29 августа) — должен
отказать.

---

## P0.4 Брошенные рабочие каталоги сборки

> **Реализовано 2026-09-14** (`cmd/ang/build_scratch_sweep.go`,
> `process_alive_unix.go`, `process_alive_windows.go`). Живость — сигнал 0 на unix
> (`EPERM` тоже значит «жив»); на windows надёжной проверки нет, и владелец всегда
> считается живым — лучше оставить мусор, чем удалить идущую сборку. Уборка —
> best effort и никогда не роняет сборку; итог печатается в логе сборки, каталоги
> с `keep` — отдельным предупреждением. Тесты: `TestSweepAbandonedBuildScratch`,
> `TestBuildTransactionRecordsOwnerAndSweepsAbandonedScratch`.
>
> **На dealingi-back не запускалось сознательно:** первая сборка новым бинарником
> удалит все 17 старых каталогов (657 МБ, все старше суток, без `owner.json`).
> Это решение владельца проекта.

**Проблема.** Сборка, убитая сигналом или упавшая до `Rollback`, не удаляет свои
`.ang-build-transaction-*` (рядом с принадлежащими путями) и
`.ang-build-workspace-*` (рядом с корнем проекта). `.gitignore` их скрывает, и они
копятся. В dealingi-back на 2026-09-14: 13 транзакций и 4 рабочие копии,
**657 МБ**, самая старая — 2026-08-09; каждая копия содержит снимок `tests/`
(91 МБ, там `node_modules`).

**Требуемое поведение.**
- При создании рабочего каталога пиши в него `owner.json`: PID, имя хоста, время.
- В начале сборки находи каталоги со своими префиксами в тех же местах и удаляй
  те, чей владелец мёртв: хост совпадает, а процесса с этим PID нет. Каталоги без
  `owner.json` (старый формат) удаляй, только если они старше 24 часов.
- Никогда не трогай каталог живого процесса и каталог с другого хоста.
- Сообщай одной строкой, сколько удалено и сколько места освобождено.
- Каталоги, оставленные `keepWorkDirs` после неудачного отката (там оригиналы),
  **не удалять автоматически** — пометь их в `owner.json` и напечатай путь.

**Приёмка.** Тест: каталог с PID несуществующего процесса удаляется; с PID
текущего процесса — нет; без `owner.json` младше 24 часов — нет; с флагом
`keep` — нет.

## P1. Генерация воспроизводима, и это проверяется одной командой

> **Сужено и реализовано 2026-09-14.** Проверка воспроизводимости в CI ANG
> **уже есть** — план ошибочно предлагал её добавить: `.github/workflows/ci.yml`,
> джоба `determinism-matrix` (ubuntu/macos/windows, `scripts/determinism_ci`:
> фазы all/plan/apply дважды со сравнением манифестов) и шаги
> `architecture-invariants` («Generated Artifacts Are Committed», release-сборка
> дважды со сверкой sha256, `scripts/ci_determinism_plan_apply.sh`). Новой джобы
> не добавлено.
>
> Новое — только `ang build --check` (`cmd/ang/build_check.go`): dry-run, ничего не
> пишет, выход 1 и список `+ создан` / `~ изменён`, если генерация изменила бы хоть
> один файл. Ограничение: закоммиченный файл, который генератор перестал
> выпускать, не обнаруживается — dry-run обходит только сгенерированное, а
> отличить такой файл от ручного без списка эмитированных файлов нельзя.
> Тест: `TestRunBuildCheckDetectsDriftAndWritesNothing`.

**Проблема.** Требование «дважды собрал — ноль диффа» сейчас проверяет только CI
проекта. У генератора нет собственной проверки, а история проекта помнит случаи,
когда `make generate` не воспроизводил закоммиченный код и менял контракт API.

**Требуемое поведение.**
- `ang build --check`: генерация во временный корень (можно поверх механизма
  `--dry-run`), сравнение с текущими файлами по списку из P0.1, вывод отличий
  (пути, `+`/`-`/`~`), код выхода 1 при любом отличии, 0 — при полном совпадении.
  Ничего в проект не пишет.
- Детерминизм: любой обход `map` в эмиттерах и шаблонах перед выводом сортируется;
  в выходе нет времени сборки, случайных идентификаторов, абсолютных путей машины.
- В CI самого ANG: джоба берёт фикстуру (минимальный проект в `cmd/ang/testdata`
  или `examples/`), собирает дважды в два каталога и сравнивает деревья байт в байт.

**Шаги.**
1. Реализуй `--check` как режим поверх `--dry-run`: после генерации во временный
   корень — сравнение, отчёт, код выхода.
2. Найди источники недетерминизма: `grep -rn 'range .*map\|for .* := range' compiler
   templates` и точки, где вывод формируется из `map` без `sort`. Исправляй по факту,
   не переписывай обход всех циклов вслепую.
3. Добавь CI-джобу.

**Приёмка.** `cd dealingi-back && make generate && bin/ang build --check` → код 0.
Джоба ANG зелёная.

---

## P2. Короткий цикл: увидеть изменение до записи, пересобирать только нужное

### P2.1 `--dry-run` показывает содержимое, а не только имена

> **Реализовано 2026-09-14.** `ang build --dry-run --diff` печатает unified diff
> (`github.com/pmezard/go-difflib`, 3 строки контекста; новые файлы — от
> `/dev/null`; бинарные — одной строкой) вместо JSON-манифеста. `--diff-out <dir>`
> пишет `<dir>/<путь>.patch`. С `--check` — сначала список расхождений, затем
> диффы, всё в stderr. Оба флага требуют `--dry-run` или `--check`. MCP
> `ang_dry_run` принимает `diff: bool` и возвращает поле `diffs` (патч до 200 КБ,
> всего до 2 МБ, остальное — списком без содержимого). Удалённые файлы не
> показываются — то же ограничение, что у `--check`.
>
> Проверено на dealingi-back: ручная правка в `auth__register_user.gen.go` дала
> ровно один патч, откатывающий её; JSON-манифест при `--diff` не печатается.

**Проблема.** Манифест `dry_run.go` говорит «update», но не говорит что. Агент не
может оценить правку CUE, не записав результат в проект.

**Требуемое поведение.**
- `ang build --dry-run --diff` печатает unified diff (формат `git diff --no-index`)
  для каждого файла со статусом `update`; для `create` — весь файл как добавленный;
  для удаляемых — как удалённый. Порядок файлов отсортирован.
- `--diff-out <dir>` пишет то же в `<dir>/<путь>.patch` вместо stdout.
- MCP `ang_dry_run` получает параметр `diff: bool`, возвращает диффы в ответе
  (ограничь размер, например 200 КБ на файл, остальное — «truncated, see
  --diff-out»).

**Приёмка.** Тест: фикстура, изменение одной строки в CUE, `--dry-run --diff`
содержит ровно один hunk с этой строкой.

### P2.2 Инкрементальная сборка

> **Сначала сделано ускорение, а не кеш (2026-09-15).** Профиль dry run на
> dealingi-back: из ~41 с 34 с уходило на `imports.Process` в эмиттере
> `Service Impls` (было 42 с на полной сборке). Причина: шаблоны методов не
> импортируют пакеты, которые используют тела из CUE (`strconv`,
> `crypto/sha256`, `internal/pkg/*`). goimports ищет их, **для каждого файла
> заново** разбирая все `.go` в каталоге юнита (740 файлов `internal/service`),
> а иногда ещё и обходя модули, — O(n²). Юнит передаётся относительным путём,
> то есть каталог — это старые файлы проекта на диске.
>
> `compiler/emitter/go_format.go`: `withPreviousImports` берёт из прошлого
> вывода того же юнита импорты, покрывающие неразрешённые ссылки, и добавляет их
> так же, как это делает сам goimports (удалить неиспользуемые, добавить по
> порядку путей через `astutil`). goimports тогда заканчивает на первом проходе.
> Если прошлого файла нет или он покрывает не всё — прежний полный путь.
> Проверено: все 1117 вызовов форматтера байт-в-байт совпали с прежним выводом,
> `Service Impls` 42 с → 5 с, `--check` 2094/2094, `go test ./...` зелёный.
> Зависимость от прошлого вывода не новая: goimports и раньше выбирал импорты по
> старым соседним файлам. Известный край: если соседний файл объявит глобальную
> переменную с именем пакета, который прошлый вывод импортировал, быстрый путь
> оставит импорт, а полный — нет; такое ловит `go verify`.
>
> Хеш-кеш ниже **не делался**: при сборке в ~11 с (`--check`) выигрыш мал, а
> `BuildFingerprint` не включает ревизию компилятора, и `inputHash` покрывает
> только CUE (не `ang.yaml`, `go.mod`, шаблоны проекта, `ANG_*`), так что кеш по
> описанию ниже был бы небезопасен. Возвращаться к нему — только расширив хеш.

**Проблема.** Любая правка — полная генерация всех целей. На эталонном проекте это
минуты.

**Требуемое поведение.**
- Сборка хеширует входы каждого эмитируемого файла (CUE-фрагменты и шаблоны, от
  которых он зависит) и хранит хеши в `.ang/cache` (каталог уже существует —
  см. `template_hash.go`). Файлы с неизменившимися входами не перегенерируются и не
  переписываются.
- Семантика результата **не меняется**: инкрементальная сборка обязана давать
  байт-в-байт тот же результат, что полная. `--no-cache` принудительно делает
  полную.
- Это важнее, чем «пересобрать один сервис»: сквозные файлы (маршруты,
  `common.go`, индекс SDK) зависят от всех сервисов, и любая «частичная» сборка по
  именам сервисов либо забудет их, либо сведётся к тому же графу зависимостей.
  Не делай флаг `--only service=`.

**Приёмка.** Тест: полная сборка, затем правка одного `impl_*.cue`, инкрементальная
сборка — переписаны только файлы этого сервиса и сквозные; результат совпадает с
результатом полной сборки после той же правки (сравнение деревьев).

---

## P3. Go внутри CUE: форматирование и проверка типов без полной сборки

### P3.1 `ang fmt` форматирует встроенный Go

> **Реализовано 2026-09-15** (`cmd/ang/fmt_embedded_go.go`,
> `fmt_embedded_go_decode.go`). Поля: `func` (выражение), `code` (операторы) и
> определения `#…`, если в них литерал функции; `sql` и прочие строки не
> трогаются. Форматируется только поле, значение которого — одна многострочная
> строка (конкатенация `""" + #Block + """` — не Go сама по себе). Интерполяции
> `\(…)` на время gofmt заменяются идентификаторами той же длины.
>
> Две вещи, которых план не предвидел:
> - **Сгенерированный код ссылается на строки CUE** (`// Source: file.cue:N`,
>   3071 раз в dealingi-back). Поэтому фрагмент, у которого gofmt поменял бы
>   число строк (раскрыть `if err != nil { return out, err }`), не трогается;
>   такие пропуски печатаются одной сводной строкой, поштучно — в `--json`.
> - **gofmt видит исходный текст CUE, где `\"` и `\\` ещё экранирование.** Результат
>   принимается, только если декодированный (`literal.Unquote`) Go до и после
>   совпадает токен в токен; иначе фрагмент пропускается с предупреждением.
>
> `--go-only`: только встроенный Go, без переименования действий и `cue fmt`.
> Нужен для проектов, чей CUE не в раскладке `cue fmt`: на dealingi-back
> `cue fmt` сам по себе меняет 589 из 646 файлов (у 24 — число строк). Без `cue`
> в PATH `ang fmt` раньше падал; теперь то же форматирование выполняется
> библиотекой (`cue fmt` = `parser.ParseFile` + `format.Node`).
>
> **Приёмка на копии dealingi-back:** `ang fmt --go-only` отформатировал 65
> фрагментов в 52 файлах, 238 оставил ради числа строк, 5 не разбираются до
> декодирования экранирования; повторный запуск ничего не меняет. `build --check`
> после этого: 306 файлов отличаются, и **все 626 изменённых строк — провенанс**
> (`// InputHash:`, `sha:` исходных `.cue`, `x-ang-input-hash`), ни одной строки
> кода. Критерий «форматирование входа не меняет выход» в буквальном виде
> недостижим для любой правки байтов CUE — хеши входа меняются всегда; проверяемый
> критерий: пересборка отличается только строками провенанса.

**Требуемое поведение.**
- `ang fmt` находит строковые литералы `"""…"""` в полях, которые генератор трактует
  как Go (ключ `code`, определения `#…Func`, значения `expr`/`value` действий
  `math.Expr`, `logic.Check` и т. п. — точный список возьми из
  `cmd/ang/actions.go` и `compiler/impl_sidecar.go`), и прогоняет их через
  `go/format`. Фрагмент, который не парсится как выражение/оператор/функция,
  оставляется как есть — с предупреждением, не ошибкой.
- Отступ фрагмента внутри CUE сохраняется; сама структура CUE не меняется.
- Однострочные `"…"` не трогать: их нельзя переформатировать без изменения
  семантики строки.

**Приёмка.** Тест: файл с фрагментом без отступов → после `ang fmt` отформатирован,
повторный `ang fmt` ничего не меняет. На `dealingi-back`: `ang fmt` → сборка →
`bin/ang build --check` даёт 0 (форматирование входа не меняет выход).

### P3.2 `ang vet logic --types`: проверка типов за секунды

> **Реализовано 2026-09-15** (`cmd/ang/vet_logic_types.go`). Иначе, чем
> описано ниже, — проще и без копии проекта:
> - генерация — обычный dry run в процессе (вывод и предупреждения глотаются,
>   показываются только при провале);
> - компиляция — `go build -overlay` по сгенерированным `.go` из dry run: проект
>   на диске не трогается, неизменённые пакеты берутся из кэша сборки. Пакеты —
>   `--packages`, по умолчанию `./internal/service/...`. «Один сервис» отдельно не
>   собрать: в dealingi-back все 17 сервисов — один пакет `service`;
> - привязка к CUE — **без карты строк**: в файлах методов её нет (есть только
>   заголовок со списком `.cue`), а встроенный Go копируется почти дословно.
>   Строка ищется по тексту с оценкой по трём соседним строкам с каждой стороны:
>   сначала в `.cue` из заголовка, потом по всему дереву CUE; колонка пересчитывается
>   по отступу. Нет однозначного совпадения (ошибка в каркасе, текст изменён
>   экранированием CUE) — печатается сгенерированное место и `.cue`, из которых
>   файл сделан.
>
> Попутно: старое `mapGoBuildErrorsToCUESource` в go verify ищет комментарий
> `// Generated from:`, которого текущие заголовки не пишут, — оно не срабатывает;
> не трогал.
>
> **Приёмка на копии dealingi-back:** в `impl_tender_get.cue` вызов
> `s.TenderProductRepo.ListByTender` заменён на несуществующий `ListByTenderNope` →
> `cue/api/impl_tender_get.cue:46:41: … has no field or method ListByTenderNope`
> (совпадение exact) + `generated: internal/service/tender__get_tender.gen.go:71:37`,
> 10.4 с (генерация 9.5, компиляция 0.8). Первый прогон после смены пакета
> компилирует ~6 с, дальше кэш. Цель ≤ 10 с — на границе; остаток — генерация
> всего проекта (P2.2 уже сократил её с ~41 до ~10 с).

**Проблема.** `vet logic` ловит только синтаксис. Неверное имя поля, неверная
сигнатура `(int64, error)` у finder-а, лишний аргумент — всё это всплывает только
после полной генерации и `go build`.

**Требуемое поведение.**
- Режим `--types`: для каждого сервиса генератор собирает **только** пакет этого
  сервиса и его прямые зависимости (порты, домен, DTO) во временный каталог поверх
  overlay-workspace из P0, и запускает на нём `go/types` (`golang.org/x/tools/go/packages`
  с `NeedTypes`) или, если это проще внедрить, `go vet ./internal/service/<pkg>/...`
  в workspace. Полная генерация всех целей, фронта, тестов и `go build ./...` не
  выполняется.
- Диагностика привязывается обратно к CUE: `cue/api/impl_x.cue:LINE:COL` фрагмента,
  а не к сгенерированному файлу. Для этого эмиттер должен сохранять карту
  «сгенерированная строка → файл/строка CUE» (посмотри, есть ли уже что-то похожее в
  `compiler/emitter/service_flow_codegen_ast_helpers.go`).
- Цель по времени на эталонном проекте: секунды на один сервис, не минуты.

**Приёмка.** Тест: фрагмент вызывает несуществующий метод репозитория → `vet logic
--types` печатает ошибку с координатами в `.cue`. На `dealingi-back`: время
`ang vet logic --types` для одного сервиса ≤ 10 с.

---

## P4 (по желанию). Реализации в настоящих `.go`-файлах

> **Отклонён (2026-09-15).** Решение пользователя: Go из CUE не выносится — ни в
> `internal/`, ни в отдельные `.go`-файлы (`funcRef`). Вместо этого — P5.1: сырые
> строки, полный gofmt, ошибки типов в редакторе.

Идея: вместо `code: """…"""` разрешить `code: "@file(impl/x.go#FuncName)"` —
генератор читает функцию из обычного Go-файла и вставляет её тело. Тогда работают
gofmt, gopls и компилятор напрямую. Это меняет формат входа и требует миграции 162
файлов — делай только после P0–P3, отдельной веткой, с обратной совместимостью
(оба способа работают одновременно) и без миграции эталонного проекта в той же
ветке.

---

## P5. Чтобы агент работал с ANG с удовольствием, а не через силу

Написано 2026-09-15 после выполнения P0–P3 — по тому, обо что агент спотыкался на
эталонном проекте в течение этой работы. Каждый пункт начинается с цифр, потому что
без них легко сделать не то. Порядок пунктов — по пользе; порядок *выполнения* и
решения пользователя — в конце раздела.

### P5.0 Факты, которых не видно из кода (проверены 2026-09-15)

**Сколько Go живёт внутри CUE в dealingi-back.** 27 823 строки Go в
`"""…"""`-строках: 477 блоков `func:` (литералы `logic.Call`), 27 блоков `code:`
(тела методов `impls: go`), 63 определения `#…Func` и 51 другое `#…`-определение с
Go внутри. 1 665 строк содержат экранированные кавычки `\"`. 329 раз Go-текст
собирается интерполяцией `\(#ИмяFunc)(…)` — это макросы, подставляемые *текстом*.
5 блоков вообще не разбираются как Go, пока CUE не раскодирует экранирование.
Из 427 разобранных литералов 343 обращаются к `s.` — получателю метода, чьи поля
(`s.txManager`, `s.TenderProductRepo`) частично неэкспортированы — поэтому hover из P5.1
берёт типы и поля из пакета `service` генерата.

**Что уже умеет генератор — не изобретай заново.**
- `logic.Call` с `func: "имя"` (без литерала) рендерится как вызов
  `имя(args…)` — `compiler/emitter/service_flow_codegen_typed_core.go:174`,
  `renderTypedCallResult`. В dealingi-back это уже используется 49 раз
  (`func: "s.ProductImageSyncJobRepo.ListStaleQueued"`).
- Два вида sidecar-файлов: `impls: go: funcRef: "x.go#Name"` (тело метода из
  top-level функции — `compiler/impl_sidecar.go:resolveGoSidecarImpls`) и
  `logic.Call` с `funcRef` (литерал функции — `resolveLogicCallSidecars`, импорты
  через `_funcRefImports`, потребляются в `emitter/service.go:555`). В dealingi-back
  **0 использований**. Sidecar-файл там — просто текст: он не обязан компилироваться,
  поэтому gopls в нём не работает. Пользователь решил sidecar-файлы не использовать
  (P5.1).
- Позиции есть: `normalizer.FlowStep` (модуль `ang-ir`, `normalizer/types.go:255`)
  несёт `File`, `Line`, `Column`, `CUEPath`; шаблон метода печатает
  `// Generated from: {{ .Method.Source }}` (`templates/service_impl_method.tmpl`).
  Директив `//line` в выходе нет (0 штук в 2094 файлах).
- Хеш входа: `ComputeProjectHashWithRoot` (`compiler/pipeline.go:25`) — sha256 всех
  `.cue` под корнем CUE. Штампуется через funcmap `InputHash` в 13 шаблонах
  (`domain`, `http`, `openapi`, `asyncapi`, `postgres_repo`, `mongo_*`,
  `websocket*`, `projections`, `aiact`, `main_server*`), в эмиттерах
  `domain.go`, `gdpr.go`, `aiact.go`, `app.go` и как `x-ang-input-hash` в
  `transport_specs.go`. **Он же уже записан в `ang-manifest.json`**
  (`writeArtifactHashManifest`, `cmd/ang/artifact_manifest.go:45`). Шапка
  сервисных файлов (`renderGeneratedHeader`, `service.go:774`) содержит sha
  *собственных* источников — её трогать не нужно.
- Предупреждения гасятся только построчно: `//ang:nolint КОД` в CUE в пределах двух
  строк выше (`cmd/ang/validate.go:215 diagnosticSuppressed`). `ang.yaml` читается
  тремя ключами (`cue_root`, `templates_dir`, `schema_dir` —
  `loadProjectConfig`). Ни уровня проекта, ни причины подавления нет.
- Каталог действий `ang actions --json` берёт описания из `ang-ir/flowsem/catalog.go`,
  а поле `example` там — автозаглушка из имён аргументов:
  `{ action: "repo.Query", method: "<expr>" }`. Реальная форма в проекте —
  `source`, `method`, `args: […]`, `output`, `list: true`. У `flowir.ActionSpec`
  (`compiler/flowir/registry.go:162`) поля `Example` нет. Настоящие примеры на
  каждое действие уже лежат в
  `compiler/emitter/service_flow_codegen_action_matrix_test.go`.
- `ang-ir` — отдельный модуль (`go.mod`: `github.com/strogmv/ang-ir v0.0.0-20260725…`).
  Всё, что меняет `FlowStep`, `Method` или каталог, — коммит туда плюс bump здесь.

**Замеры (dealingi-back, 2026-09-15, после P2.2).** `ang build --check`: CUE + IR
1,6 с; эмиттеры 8,1 с, из них `Service Impls` 5,0 с, `Port Mocks` 0,9 с,
`Postgres Repos` 0,8 с, `Repository Mocks` 0,5 с, остальные 34 шага — меньше 0,3 с
каждый. Настоящая сборка добавляет ~4,4 с *до* эмиттеров (снимок + рабочий
каталог) и ~21,8 с *после* (go verify ≈ 17 с, слияние, манифест). `ang vet logic
--types` ≈ 10 с — это ровно 1,6 + 8,1.

**Шум.** Каждая сборка печатает 153 предупреждения: 63 `LARGE_CUE_FILE`,
37 `SHARED_ARCH_AUDIT`, 27 `SHARED_ARCH_UNDERUSED`, 10 `RAW_GO_EXPR_IN_ASSIGN`,
5 `ORPHAN_PUBLISH`, 5 `LAMBDA_BARE_ERROR`, 2 `MISSING_PUBLISH`,
2 `EVENT_PUBLISH_RAW_PAYLOAD_FORBIDDEN`. Ни одно не меняется от сборки к сборке.
`LARGE_CUE_FILE` (лимит 300 строк, `pipeline_diagnostics.go:100`) считает строки
*вместе со встроенным Go* — то есть ругается на Go, а не на CUE. Текстовый dry-run
печатает 725 строк `Generated Service Impl Method: …` в stdout.

**Как выглядит настоящая ошибка.** Незакрытая строка в Go-блоке: сборка честно
падает с кодом 1, событие `{"status":"error","code":"INVALID_GO_EXPR","cueFile":
"cue/api/impl_tender_get.cue","line":39,"column":4,…}` — координаты есть. Но оно
84-е из 158 строк лога, **после него ещё 70 предупреждений**, а `message` содержит
всю функцию целиком (сотни символов в одной строке). Агент видит в конце лога
предупреждения и `Build FAILED due to diagnostic errors.` и идёт искать причину grep-ом.

**Ошибка генератора, найденная попутно.** `CleanImplCode`
(`compiler/emitter/service.go:853`): `strings.ReplaceAll(line, "l.", "slog.")` —
задумано для логгера `l` (`l.Info` → `slog.Info`), а заменяет *любое* `l.`:
`html.EscapeString` → `htmslog.EscapeString`, `url.Parse` → `urslog.Parse`,
`model.Name` → `modeslog.Name`. Действует на блоки `impls: go: code` (27 в проекте);
в них поэтому нельзя использовать `html`, `url` и любую переменную на `l`.
Выход проекта сейчас совпадает только потому, что таких идентификаторов там нет.

### P5.1 Go внутри CUE — без экранирования, отформатированный, с ошибками прямо в редакторе

> **Уточнение по цифрам (2026-09-15, после `ang fmt` проекта).** Экранирование
> в Go-блоках осталось только в ~40 блоках (`\"` — в 6, `\\` — в 34); из 6060
> вхождений `\"` во всём CUE 6057 — в **однострочных** строках шагов
> (`condition: "req.ID != \"\""`). Перевод Go-блоков в `#"""…"""#` полезен
> (убирает ловушку `\\n`, позволяет `"a\"b"`), но основной шум — в
> однострочных выражениях; их перевод в `#"…"#` — отдельное решение (читаемость
> `#"req.ID != """#` спорна), не делать молча.

> **P5.1.1–2 сделаны (2026-09-15).** `ang fmt --raw-go` переводит `func`/`code`/
> `#…Func` в `#"""…"""#`: куски строки декодируются по контракту CUE
> (`QuoteInfo.Unquote` — без открывающего маркера, с закрывающим), выражения
> интерполяций переносятся как `\#(…)`, новый литерал заново разбирается и
> сверяется со старым. Форматирование встроенного Go теперь по умолчанию
> полное (`--keep-lines` возвращает прежнее поведение); проверка токенов
> не учитывает `;` — иначе раскрытый `if … { return }` считался изменением.
> Копия dealingi-back: 575 из 588 Go-полей стали сырыми, 606 фрагментов,
> 0 пропусков, `ang fmt --check` сразу после — 0, сборка зелёная; из
> 132 изменившихся сгенерированных файлов 131 отличаются только позициями, в
> одном появилась пустая строка, которая и так была в CUE (потоки Go-токенов
> совпадают: 615 токенов).
> dealingi-back переведён (2026-09-15): `b2ac8063` — лок `a261a56`, `bbcc47be` —
> `bin/ang fmt --raw-go` (236 файлов, 606 фрагментов, 0 пропусков); проверено
> сравнением генерата без позиций и потоков Go-токенов для единственного
> файла с пробельным отличием. Осталось P5.1.3–4 (ошибки типов и hover в LSP).

> **Решение пользователя (2026-09-15).** Рукописного Go в `internal/` не будет —
> ни в `.gen.go`, ни рядом: `internal/` всегда пересобирается, правда живёт только
> в CUE, а всё, что должно измениться в генерате, меняется через генератор и
> шаблоны. Sidecar-файлы (`funcRef`) тоже не использовать. Поэтому улучшаем сам Go
> внутри CUE.

**Проблема.** Из P5.0: 27 823 строки Go в `"""`-строках, 1 665 строк с `\"`,
5 блоков не разбираются как Go, пока CUE не раскодирует экранирование. Ловушка из
истории проекта: `\n` внутри `"""` — это настоящий перевод строки, Go-литерал
получается незакрытым, нужно писать `\\n`. Ошибку типа видно только после полной
сборки (P3.2 даёт её за ~10 с, но из терминала, а не на месте).

**Проверенный факт (cuelang.org/go v0.17.1, 2026-09-15).** В обычной `"""`-строке
`\"` декодируется в голую кавычку — Go `"a\n\"b\""` в ней записать **невозможно**:
получится `"a<перевод строки>"b""`. В сырой строке `#"""…"""#` обратная косая не
интерпретируется: Go лежит байт в байт, как в `.go`-файле, а интерполяция
пишется `\#(#ИмяFunc)`. Проба: одна и та же строка Go в двух формах — сырая
сохраняет `\"` и `\n` дословно.

**Что сделать.**
1. **Перевод в сырые строки** — `ang fmt --raw-go` (и то же поведение внутри
   `--go-only`, когда флаг задан). Для полей, которые форматирует P3.1 (`func`,
   `code`, `#…` с литералом функции):
   - новое содержимое = **декодированное значение** строки: для каждого
     `BasicLit`-сегмента — `literal.Unquote`, каждая интерполяция `\(выражение)`
     → `\#(выражение)` с тем же текстом выражения (сегменты брать из
     `ast.Interpolation.Elts`, не из текста);
   - отступ содержимого и закрывающих кавычек — как у исходной строки;
   - пропустить с предупреждением, если в значении встречается `"""#` или `\#`
     (иначе сырая строка закроется или начнёт интерполяцию раньше);
   - проверка на каждый блок **до записи**: декодированные сегменты новой строки
     равны декодированным сегментам старой, выражения интерполяций равны по
     тексту. Значение строки не меняется, значит выход сборки обязан совпасть
     (кроме строк провенанса, пока не сделан P5.4).
   `sql` и прочие строки не трогать. Три блока, собранные конкатенацией
   `""" + #Block + """`, не трогать (см. P5.0).
2. **Полный gofmt блоков** после перевода — без ограничения «число строк не
   меняется» из P3.1. Ограничение появилось из-за `// Source: file.cue:N` в
   генерате; сдвиг номеров строк — обычное следствие любой правки CUE и
   фиксируется одним коммитом. Старое поведение оставить флагом `--keep-lines`
   для точечных правок. Проверка токенов после декодирования
   (`sameDecodedGoTokens`) остаётся.
3. **Ошибки типов в редакторе.** В `cmd/ang/lsp.go` на `textDocument/didSave`
   (не на каждое изменение) запускать движок `ang vet logic --types`
   (`typeCheckEmbeddedGo` из `cmd/ang/vet_logic_types.go`) в фоне, с отменой
   предыдущего запуска, и публиковать результат диагностиками на строках CUE
   (`source: "ang-types"`). Нынешний `collectDiagnosticsByURI` копирует только
   `cue/` во временный каталог — компилятору нужен весь проект, поэтому здесь
   работать от корня workspace: dry run во временный каталог + `go build
   -overlay`, проект не трогается (так уже устроен P3.2). Несохранённые буферы не
   учитывать — проверка идёт по сохранённым файлам. Время — ~10 с на сохранение
   (после P5.6 — ~3 с).
4. **Hover внутри Go-блока** для `s.<Имя>Repo.<Метод>`, `port.<Тип>`,
   `domain.<Тип>`: сигнатура метода интерфейса порта или поля структуры, взятые
   из генерата через `golang.org/x/tools/go/packages` (загружать один раз на
   сборку и кешировать по mtime генерата). Это делает видимыми на месте правила
   вроде «delete-finder возвращает `(int64, error)`» и имена
   `<Op>ResponseData`/`<Op>Response<Поле>Item` без grep по проекту.

**Что не делать.** Не выносить Go из CUE — ни в `internal/`, ни в `funcRef`-файлы.
Не менять значения строк при переводе в сырые. Не делать проверку типов на каждое
нажатие клавиши. Не трогать `funcRef`-механизм и его тесты — он остаётся для
других проектов.

**Приёмка.**
- Тест ANG: блок с `\"`, `\\n` и `\(#X)` → `#"""…"""#` с `"`, `\n` и `\#(#X)`;
  значение CUE до и после совпадает (скомпилировать оба файла через
  `cuecontext` с тем же определением `#X`); повторный запуск ничего не меняет;
  блок с `"""#` в значении — пропущен с предупреждением.
- На копии dealingi-back: после `ang fmt --raw-go` в мигрированных блоках нет
  `\"`; `ang build --check` — отличаются только строки провенанса (проверка
  скриптом, как при приёмке P3.1); `go test ./...` зелёный.
- LSP (тест на уровне протокола, по образцу `cmd/ang/lsp_test.go`): после `didSave` с несуществующим методом репозитория приходит
  `publishDiagnostics` с диапазоном на строке CUE и `source: "ang-types"`.

### P5.2 Ошибки, паники и gopls указывают на CUE

**Проблема.** Компилятор, `go vet`, паника в проде и gopls показывают
`tender__get_tender.gen.go:71`. `vet logic --types` (P3.2) находит строку CUE
**по тексту** — это временная мера. Встроенный Go остаётся в CUE (P5.1), плюс
выражения шагов (`math.Expr`, условия `logic.Check`, `flow.If`) — всем им нужны
точные позиции.

**Что сделать.**
1. Сначала проверь на пятистрочном файле, что `go/printer` (gofmt) оставляет
   комментарий `//line file:N` в первой колонке внутри тела функции. Если
   сдвигает — используй форму `/*line file:N:C*/`, которую можно ставить где угодно
   (действует на следующую за ней позицию); её gofmt не портит.
2. В рендере шагов (`RenderFlow` и рендер `Impl.Code` в
   `templates/service_impl_method.tmpl`) перед кодом каждого шага/блока выводить
   **служебную метку** `//ang:src <file>:<line>:<col>` из `FlowStep.File/Line/Column`
   (для `code:` — из позиции литерала; `Method.Source` даёт файл и строку поля).
   Метка — обычный комментарий, gofmt ей не страшен.
3. После `formatGoStrict` — последний проход `finalizeLineDirectives(src, generatedPath)`:
   каждую метку заменить директивой на CUE-файл, а перед первой строкой после
   фрагмента вставить директиву обратно на сгенерированный файл
   (`//line internal/service/x.gen.go:N`, где N — номер этой строки в *итоговом*
   файле; считай в одном проходе с учётом вставленных строк). Границу фрагмента
   шаг знает сам (число строк своего кода). Проход — единственный, кто пишет
   директивы; gofmt после него не запускается.
4. `vet logic --types`: убрать сопоставление по тексту, читать позиции из вывода
   компилятора как есть (он уже будет печатать `cue/api/impl_x.cue:46:41`). Старую
   `mapGoBuildErrorsToCUESource` в `build_go_verify.go` (ищет `// Generated from:`,
   которого шапки не пишут) — заменить на то же.

**Что не делать.** Не выводить директивы из шаблонов до gofmt (см. п. 1). Не
ставить директивы в файлы, где нет встроенного Go (порты, DTO, репозитории) — там
нечего отображать. Не менять `FlowStep` ради этого — поля уже есть.

**Приёмка.** Тест: шаг с `math.Expr`, ссылающийся на несуществующее поле →
`go build` в сгенерированном проекте печатает `cue/api/….cue:LINE:COL`, и
`vet logic --types` печатает то же без своего поиска. На dealingi-back: паника,
искусственно вставленная в блок `func:` копии проекта, в стеке показывает
`impl_tender_get.cue`. `--check` на неизменённом проекте после разового
перегенерирующего коммита — чист.

> **Сделано (2026-09-15).** ang-ir `3b9a0c7`: `ir.FlowStep` несёт `File/Line/Column/CUEPath`
> и `ArgText` (где начинается текст многострочного литерала-аргумента) — с
> `json:"-"`, сериализованный IR не меняется. До этого позиции терялись в
> `ConvertFlowSteps` и `irFlowStepsToNormalizer`, и комментарии `// Generated from:`
> у шагов не выводились вообще. Рендер шагов пишет метку `//ang:line файл:строка
> число-строк`; `finalizeLineDirectives` после `formatGoStrict` заменяет её
> директивой: для `logic.Call` с литералом `func` — строка начала текста и все
> его строки (Go литерала совпадает с CUE построчно, это держит `ang fmt --check`),
> для остальных шагов — только первая строка кода шага; затем директива обратно
> на `.gen.go` с его настоящим номером. Пути — от места файла в проекте
> (`Emitter.SourceBackendDir`), а не от рабочей копии сборки. На dealingi-back:
> 7197 директив на CUE, 5643 возврата, код не меняется. Проверено: ошибка типа в
> блоке `func` → `impl_user_avatar_write.cue:14` (точная строка); паника в
> отображённом коде → в стеке CUE-строка; `vet logic --types` берёт CUE-позицию из
> вывода компилятора (с `-overlay` Go считает путь от копии — переводится обратно в
> проект), поиск по тексту остался только для блоков `code:` — у них
> `cleanImplCode` выкидывает строки, директивы для них не ставятся.
> `build_go_verify` не менялся: строки с `.cue` проходят как есть.

### P5.3 Предупреждения: исправить причины, а вывод сделать читаемым

> **Сделано в ANG и ang-ir (2026-09-15), коммиты `da15749` и следующий; ang-ir
> ветка `agent-ergonomics-p5`, `cf05929`.** Проверки исправлены: LARGE_CUE_FILE,
> shared_arch (правило и Go-блоки; реестр — в `ang vet`), события (шаги
> публикации, WebSocket-потребители, EVENT_PUBLISH_UNDECLARED, места),
> RAW_GO_EXPR_IN_ASSIGN (согласовано с normalizer), LAMBDA_BARE_ERROR (перенесена
> в ANG, только HTTP-операции, место операции как запасное), новая
> TAUTOLOGICAL_CHECK. На dealingi-back было 153 предупреждения — стало 124, у всех
> есть место: 90 TAUTOLOGICAL_CHECK, 14 LARGE_CUE_FILE, 8
> EVENT_PUBLISH_UNDECLARED, 4 ORPHAN_PUBLISH, 4 SHARED_ARCH_UNDERUSED, 2
> EVENT_PUBLISH_RAW_PAYLOAD_FORBIDDEN, 1 LAMBDA_INLINE_ESCAPE, 1 LAMBDA_BARE_ERROR
> (`Tender.CreateTender`). Всё оставшееся — настоящие правки CUE.
>
> **dealingi-back, 2026-09-15: 0 предупреждений.** По коммиту на класс: 90
> тавтологий удалены вместе с ненужными `output:`; 7 операций объявили
> `publishes:`; события без потребителя удалены (вместе с шагами `time.Now` /
> `time.Format`, которые питали только их — `go verify` это ловит); 2 `payload`
> → `payloadMap`; однострочная лямбда переписана; 4 пометки `shared_arch`
> сняты; `CreateTender` — авторский `//ang:nolint`, заработавший после
> исправления позиций шагов в ang-ir (`f983e20`: у шага с интерполяцией
> позиция бралась из самого шага и терялась, теперь — из поля `action`);
> 14 больших файлов разрезаны по AST (`*_partN.cue`, `list.Concat` для двух
> длинных потоков). Генерат при разрезке менялся только в позициях.
>
> **Вывод сборки — сделан (2026-09-15).** Текст: одиночное предупреждение как
> раньше, повторяющиеся свёрнуты по коду (`⚠️  WARN [КОД] ×N` + три места),
> ошибки последними строкой `❌ файл:строка:колонка: ERROR [КОД]: первая
> строка` (до 300 символов), итог `N warnings, M suppressed, K errors`, затем
> `Build FAILED: N error(s); first: …` или `Build SUCCESSFUL in 41.2s.`. JSON:
> ошибки последними, `message` — первая строка, полный текст в `detail`,
> последним событием — итог провала. Строки `Generated …` (99 вызовов в
> эмиттерах) и полный список — только с `--verbose`. Разбор логов в `ang doctor`
> понимает старое `at cue/…`, новое `❌ cue/…:N:M:` и JSON `cueFile`/`line`.
> Важно: при перенаправленном выводе (агент, CI) `ang build` всегда пишет JSON
> (`!stdoutIsTerminal()`), поэтому для агента главное — JSON-режим.

> **Решение пользователя (2026-09-15).** Предупреждения не гасить — они скорее
> всего не просто так. Подавление на уровне проекта (`ang.yaml`) **не делать**.
> Существующий построчный `//ang:nolint КОД` оставить как есть.

**Разбор всех 153 предупреждений dealingi-back (2026-09-15).** Примерно половина —
ошибки самих проверок: не видят Go-блоки, не знают про WebSocket, считают строки
Go строками CUE. Остальное — настоящие расхождения в CUE, и одна настоящая ошибка
логики, найденная попутно. Каждое исправление — отдельный коммит: в ANG или
ang-ir (с тестом «ложный случай молчит, настоящий ругается с местом») или в CUE
dealingi-back (с `make generate`, просмотром диффа генерата и `go test ./...`).

**LARGE_CUE_FILE — 63.** Считает строки встроенного Go. Без содержимого
многострочных строк больше 300 строк CUE только в 14 файлах (строк CUE без Go /
всего): `b2b_offers.cue` 663/663, `impl_b2b_offers.cue` 655/1851,
`impl_company_logo_processing.cue` 540/664, `http_channel.cue` 517/517,
`impl_blindspots_part2.cue` 481/481, `impl_attachments.cue` 479/936,
`impl_company_products_import_jobs.cue` 425/699,
`tender_workflow_transition_tender.cue` 410/410, `channel_orders.cue` 406/406,
`company_api_part4.cue` 349/349, `b2b_share_links.cue` 329/329,
`user_profile_api.cue` 321/321, `channel_catalog.cue` 320/320, `auth_api.cue`
308/308.
- ANG (`compiler/pipeline_diagnostics.go:100`): считать строки без содержимого
  `"""`/`#"""`-строк (`cue/parser`, при обходе пропускать `BasicLit` и
  `Interpolation` — так посчитаны цифры выше). Сейчас проверка смотрит только
  `*.cue` одного каталога через `filepath.Glob` — оставить.
- CUE: разрезать 14 файлов по операциям в том же пакете. Разрезка сдвигает
  `// Source:`-комментарии в генерате — отдельный коммит, после P5.4.

**SHARED_ARCH_UNDERUSED — 27 и SHARED_ARCH_AUDIT — 37.** Пометка `shared_arch`
не только для отчёта: в ang-ir `validateFlowSteps` (`normalizer/service_flow_validate.go:144`)
она снимает ошибку «доступ к сущности чужого контекста» с шагов потока. Контекст —
`bounded_context` сущности или часть имени владельца до `_`/`-`/`.`
(`inferBoundedContext`); `admin` и `audit` исключены. Поэтому правило проверки
«используется меньше чем двумя контекстами — пометка лишняя» неверно: сущность,
которой пользуется один, но **чужой** контекст, пометку требует.

Разбор всех 37 через `compiler.RunSemanticPhases` (кто обращается шагами потока,
кто — `s.<Entity>Repo.` из Go-блоков), 2026-09-15:
- **Пометка лишняя — 4:** `NegotiationSearchQuery`, `SearchDocument`,
  `TenderTemplate`, `TenderTemplateCategory` — все обращения из собственного
  контекста. CUE: снять `@shared_arch`, `--check` должен остаться чистым.
- **Пометку требует проверка границ — 15:** чужие контексты обращаются шагами
  потока (`Attachment`, `AuditLog`, `CategoryValue`, `Company`, `CompanyAddress`,
  `CompanyCountry`, `CompanyInvite`, `Consent`, `Counterparty`, `Country`,
  `DataEraseRequest`, `DataExportJob`, `EmailVerificationToken`, `Product`,
  `User`). У семи контекст выведен из имени владельца, которому не соответствует
  ни один сервис: `consent`, `data` (два), `email`, `category`, `counterparty`,
  `product`. CUE: задать им явный `bounded_context` по смыслу (например,
  `EmailVerificationToken` → `auth`, `DataExportJob`/`DataEraseRequest` →
  `user`, `Product` → `company`) и пересчитать — часть пометок станет лишней.
- **Чужие обращения только из Go-блоков — 18:** `APIKey`, `Application`,
  `B2BQuoteRequest`, `B2BQuoteRequestLine`, `B2BShareLink`, `Bid`, `BlockOrder`,
  `CatalogShareQuantity`, `CompanyCategoryScore`, `Market`, `Notification`,
  `OperationalGroup`, `PriceList`, `ProductPackage`, `ProductPackageItem`,
  `ProductPrice`, `ProductVariant`, `ProductVariantPrice`. Проверка границ Go-блоки
  не видит, но связь настоящая; пометка честная.

ANG (`compiler/pipeline_shared_arch.go`):
- считать обращения и из Go-блоков (`func`, `code` шагов и `Impl.Code` метода,
  регэксп `s\.([A-Z]\w*)Repo\.`);
- `SHARED_ARCH_UNDERUSED` → «пометка лишняя»: все обращающиеся контексты (поток и
  Go, кроме `admin`/`audit`) совпадают с контекстом сущности. Сущности без
  обращений тоже сюда;
- `SHARED_ARCH_AUDIT` — реестр долга, а не проблема сборки: убрать из вывода
  сборки, показывать в `ang vet` (сущность, контекст, reason, ticket, чужие
  контексты по потоку и по Go). Переделка общих сущностей на read model —
  отдельное решение пользователя, не часть P5.

**RAW_GO_EXPR_IN_ASSIGN — 10.** Три литерала структур из ссылок
(`port.ReleaseTenderStockRequest{CompanyID: award.WinnerCompanyID, AwardID:
award.ID}` и два похожих), два `time.Time{}`, пять `_webhookBuyer ||
_webhookSeller`. Для `payloadMap` и аргументов вызовов ang-ir **уже** считает
литералы структур из ссылок безопасными (`isSafeCallArgExpr`,
`normalizer/service_checks.go:437`), для `mapping.Assign` — нет
(`isSafeMappingAssignValue`, `flowsem/engine.go`).
- ang-ir: в `isSafeMappingAssignValue` переиспользовать `isSafeCallArgExpr` и
  разрешить `&&`, `||`, `!` над безопасными операндами. Все 10 уходят законно.
- **Попутно — идиома, а не одна ошибка.** Проверка `TAUTOLOGICAL_CHECK` (сделана,
  коммит `da15749`) нашла на dealingi-back **90** шагов вида
  `{action: "logic.Check", condition: "auditWritten || !auditWritten", throw: "audit
  log write failed"}` сразу после `logic.Call … output: "auditWritten"`. Условие
  всегда истинно, `throw` никогда не сработает. Так гасят Go-ошибку «declared and
  not used», хотя штатный способ есть: `logic.Call` без `output:` рендерится как
  `if _, err := call; err != nil { return … }` (`renderTypedCallResult`,
  `compiler/emitter/service_flow_codegen_typed_core.go:242`) — результат
  отбрасывается, ошибка проверяется. CUE: удалить 90 шагов-тавтологий и
  `output:` у порождающего вызова, если переменная больше нигде не используется
  (где используется, как `_webhookBuyer` в `resp.Ok`, — удалить только проверку).
  Проверять генерацией, `ang vet logic --types` и `go build`, а не по тексту.

**LAMBDA_BARE_ERROR — 5.** «`fmt.Errorf` в `logic.Call` даст HTTP 500». Все четыре
с местом — не HTTP: три обработчика событий (`ReindexCompanyProductCatalog`,
`ReindexProductStockQuantity` в `search`, `OnCompanyCatalogCollectionBulkRequested`
в `company` — у всех `subscribes:`) и `ImportCompanyProductsCSVStream`, который по
комментарию в `cue/api/company_products_import_stream.cue:5` — путь только для
воркера (большие CSV). Пятое предупреждение — без места. При этом `fmt.Errorf` есть в 11 литералах
`logic.Call` (`impl_company_catalog_collection_bulk_job.cue:111`,
`impl_company_products_import_stream.cue:7`, `impl_media_storage_company.cue:34`
и `:122`, `impl_media_tender_cover.cue:5`, `impl_media_user_attachments.cue:5` и
`:93`, `impl_media_video_document.cue:111`,
`impl_search_product_indexation.cue:7`, `impl_search_product_quantity.cue:7`,
`impl_tender_create.cue:17`), а предупреждений 5 — проверка непоследовательна.
- ang-ir (`normalizer/service_flow_validate_handlers_data.go:301`): не
  предупреждать в методах с `subscribes`/расписанием; проверять вложенные шаги
  так же, как верхние; всегда передавать место шага.
- CUE: пройти все 11 мест. В HTTP-методах, где ошибка — вина клиента (нет прав,
  неверный ввод, не найдено), заменить на `errors.New(http.StatusXxx, "Code",
  "Message")`. Где это внутренняя ошибка (сбой хранилища), 500 верен — тогда
  переписать через `errors.New(http.StatusInternalServerError, …)` явно, чтобы
  намерение было видно и проверка молчала.

**ORPHAN_PUBLISH — 5.**
- `CompanyCatalogCollectionBulkProgressChanged` уходит на фронт через WebSocket:
  он в `messages:` канала в `cue/api/http_part5.cue:180`. Проверка
  (`emitEventUsageDiagnostics`, `compiler/pipeline_events.go`) про WebSocket не
  знает и учитывает только ручной список `BroadcastOnly` в
  `cue/events_meta/annotations.cue`.
  ANG: события из `messages:` WebSocket-каналов считать потреблёнными.
- `SearchReindexProgressChanged` не в WS-списке и на фронте не используется
  (0 упоминаний в `dealingi-front/src`). **Решение (2026-09-15):** убрать из
  `publishes:` (`cue/api/search_reindex.cue:145,159`) вместе с шагами публикации
  и определением события — потребителя нет.
- `TenderStockReserved`, `TenderStockReleased`, `TenderStockConsumed` объявлены в
  `publishes:` (`cue/api/tender_stock_reservations.cue:62,91,107`) и в
  `cue/architecture/services.cue:64–66`, публикуются шагами в
  `impl_tender_stock_reservations.cue:143,161,182`, но их не слушает никто,
  включая фронт. **Решение (2026-09-15):** события удалить — определения,
  `publishes:`, шаги публикации и строки в `cue/architecture/services.cue`.
  Пометка `BroadcastOnly` была бы тем же подавлением. Появится потребитель —
  события вернутся вместе с ним.

**MISSING_PUBLISH — 2 и DEAD_EVENT — 1: одна причина.**
`CompanyCatalogCollectionPublished`, `B2BQuoteRequestCreated` и
`CompanyProductImportCatalogChanged` **публикуются** шагами `event.Publish`
(`impl_company_catalog_collection_lifecycle.cue:64` и `:112`,
`impl_company_catalog_composition.cue:292`, `impl_b2b_offers.cue:412`,
`impl_company_products_import_jobs.cue:646`), но операции не объявляют их в
`publishes:`, а проверка смотрит только на объявления. У двух первых есть
подписчики — значит, они работают, а предупреждение ложное; третье есть в
WS-списке.
- ANG: шаг `event.Publish` с событием, которого нет в `publishes:` операции, —
  ошибка с местом шага (`EVENT_PUBLISH_UNDECLARED`): CUE противоречит сам себе.
  Проверки ORPHAN/MISSING/DEAD строить по объединению объявлений и шагов.
- CUE: дописать `publishes:` в операции, которым принадлежат эти шаги
  (`PublishCompanyCatalogCollection`, `RollbackCompanyCatalogCollection`,
  `RefreshCompanyCatalogComposition`, `CreateB2BQuoteRequest`,
  `OnCompanyProductImportRequested` — сверить по генерату: файлы
  `company__publish_company_catalog_collection.gen.go`,
  `company__rollback_company_catalog_collection.gen.go`,
  `company__refresh_company_catalog_composition.gen.go`,
  `company__create_b2_b_quote_request.gen.go`,
  `company__on_company_product_import_requested.gen.go`). Посмотреть дифф
  генерата: может измениться `port.Publisher` и effects-middleware — это
  ожидаемо.

**EVENT_PUBLISH_RAW_PAYLOAD_FORBIDDEN — 2.** `{ action: "event.Publish", name:
"SendTelegramAction", payload: "act" }` в `impl_channel_order_paid.cue:98` и
`impl_channel_shop_cart.cue:477`. CUE: `payloadMap` по полям
`#SendTelegramAction` (`cue/events/channel_events.cue:10`).

**LAMBDA_INLINE_ESCAPE — 1.** Однострочная лямбда на 255 символов с четырьмя `;`,
`impl_chat_lists.cue:226`. CUE: разбить на многострочный литерал (после P5.1 —
сырой строкой).

**Место у каждого предупреждения.** `ORPHAN_PUBLISH`, `MISSING_PUBLISH`,
`DEAD_EVENT` и одно `LAMBDA_BARE_ERROR` сейчас приходят без файла и строки. Для
событийных — место объявления события (или первого шага публикации), для
остальных — место шага.

**Вывод сборки.**
1. Ошибки — в конце, одна строка на ошибку: `файл:строка:колонка: КОД: первая
   строка сообщения`. Полный текст выражения (сейчас — вся функция в одной
   строке) — только в `--json` или `--verbose`. Последняя строка stderr при
   провале — ошибка, а не предупреждение.
2. Предупреждения по умолчанию сворачиваются по коду: `3× ORPHAN_PUBLISH (cue/…:N,
   …)` с местами первых трёх; полный список — `--verbose`/`--json`.
3. Текстовый режим: строки `Generated …` (725 в dry run) — только под `--verbose`;
   по умолчанию одна строка на эмиттер с числом файлов и временем.
4. Итог — фиксированный блок в конце: время по стадиям (CUE+IR, эмиттеры, go
   verify), файлов создано/изменено/удалено, конфликтов, предупреждений, ошибок.

**Приёмка.**
- Тест ANG на каждую исправленную проверку: ложный случай молчит, настоящий —
  предупреждение с файлом и строкой.
- Тест на сломанном Go-блоке: последние две строки stderr — `cue/api/x.cue:L:C:
  INVALID_GO_EXPR: …` и `Build FAILED (1 error)`.
- dealingi-back после всех коммитов: `make generate` печатает 0 предупреждений;
  `--check` чист;
  `go test ./...` зелёный; `make generate | tail -8` читается без grep.

### P5.4 Правка одной строки CUE — изменение одного сервиса, а не 306 файлов

> **Сделано (2026-09-15).** Строки `// InputHash:` убраны из 11 шаблонов и
> `x-ang-input-hash` — из openapi и asyncapi; `aiact.tmpl` не тронут (там
> `input_hash` — поле журнала ИИ-вызовов). Хеш входа остаётся в
> `ang-manifest.json`. На dealingi-back — 271 файл, все изменённые строки — эти
> хеши. Никто (фронт, SDK) их не читал.

**Проблема.** Хеш *всего* входа стоит в шапке 269 файлов и в двух спецификациях
(P5.0). Правка одного символа в любом `.cue` → в git и в `--dry-run --diff` 306
файлов, и настоящее изменение в них не видно. Агент, который проверяет «что я
поменял», не может это сделать.

**Что сделать.** Убрать `InputHash` из всех 13 шаблонов, четырёх эмиттеров и
`x-ang-input-hash` из спецификаций. Оставить: `ang-manifest.json` (уже хранит
`inputHash`), шапку сервисных файлов с sha *их* источников, `ang.lock`. Если
где-то `InputHash` использовался как «версия API» для клиентов (проверь `sdk/` и
`templates/openapi.tmpl`: кто читает `x-ang-input-hash`) — заменить на
`compilerHash` из манифеста или на sha источников операции.

**Что не делать.** Не заменять на хеш «без пробелов» и прочие полумеры — любой
глобальный хеш даёт тот же эффект. Не убирать пофайловые `(sha: …)` — они как раз
локальны.

**Приёмка.** Тест: два проекта, отличающихся одной строкой в одном `.cue`, →
отличаются только файлы, порождённые этим `.cue` (плюс манифест). На dealingi-back:
разовый коммит «306 файлов, только строки хешей» (проверить скриптом, что каждая
изменённая строка — `InputHash`/`x-ang-input-hash`, как это делалось при приёмке
P3.1), затем правка одной строки `impl_tender_get.cue` → `--dry-run` перечисляет
только `internal/service/tender__*.gen.go` и манифест.

### P5.5 Правила проекта — в инструменте, а не в памяти агента

**Проблема.** AGENTS.md dealingi-back (473 строки) хорошо описывает действия и
паттерны, но не содержит ни одного из правил, которые агент выучил, ломая сборку,
и хранит в собственных заметках между сессиями. Часть из них — ошибки генератора,
часть — конвенции, часть — вещи, которые ANG может проверить сам.

**Что сделать — по одному пункту на правило.**
1. `ReplaceAll("l.", "slog.")` (P5.0, `service.go:853`) → замена только
   идентификатора `l` (`\bl\.`). Тест: `html.EscapeString`, `url.Parse`,
   `model.Name` переживают `cleanImplCode`, `l.Info` по-прежнему становится
   `slog.Info`. `--check` на dealingi-back остаётся чистым.
   > **Сделано 2026-09-15.** Баг был живым: на dealingi-back он портил 6
   > файлов — действия аудита `company.address.additionaslog.created`,
   > `company.address.postaslog.same_as_legaslog.updated` и UID набора фильтров
   > поиска `catalog.generaslog.filters` (задумано `catalog.general.filters`).
   > Тест поймал и соседний баг: `.$1ID` в Go читается как группа `1ID`, и
   > `req.UserId` превращался в `req.`; исправлено на `.${1}ID`.
2. `s.txManager` в Go-блоке без `tx.Block` в сервисе → сейчас падает `go build`
   («`s.txManager undefined`»), потому что поле инжектится только если
   `serviceImplNeedsTx` (`service_impl_ast.go:387`) находит `tx.Block`. Диагностика
   `E_TX_MANAGER_NOT_INJECTED` с подсказкой «оберните шаг в
   `{ action: "tx.Block", do: […] }`» — при рендере, по тексту блоков.
   > **Сделано (2026-09-15).** Для шагов flow уже было: `flowStepUsesTxManager`
   > инжектит поле, если в `func`/`code` шага есть `s.txManager` (f74d6af). Дыра
   > оставалась в блоке `impl: code:` метода — он смотрел только на `tx: true`.
   > ANG `cab7b02`: `implNeedsTx`/`irImplNeedsTx` видят `s.txManager` и там.
   > Диагностика не понадобилась — поле просто инжектится. dealingi-back не
   > изменился (`--check`: 2094 файла совпадают).
3. Составные уникальные индексы из блока `indexes:` домена не попадают в
   `db/schema/schema.sql` (замечено 2026-06-27 на `ProductBalance`, `Chat`).
   Сначала воспроизвести тестом; потом либо эмитировать, либо предупреждать
   `W_INDEX_NOT_EMITTED`. Молчать нельзя.
   > **Проверено (2026-09-15): уже исправлено** в ANG 24c91c7 — `sql_schema.go`
   > выводит `entity.Indexes` как `CREATE [UNIQUE] INDEX uidx_<таблица>_<колонки>`,
   > неизвестное поле или индекс без полей роняют сборку. В `schema.sql`
   > dealingi-back 55 уникальных составных индексов, среди них `chatparticipants
   > (chatid, userid)`. Заметка в памяти агента была устаревшей.
4. Правила именования, которые нельзя менять, но нужно знать, — в
   **генерируемый** документ `docs/ang/conventions.md` (в транзакции сборки, как
   любой артефакт), собранный из кода, а не написанный руками: таблица —
   `<entity в нижнем регистре>s` (`countrys`, `companys`) из функции именования
   схемы; тип элемента выходного массива — `data` → `<Op>ResponseData`, иначе
   `<Op>Response<Поле>Item`, вход — `<Op>Request<Поле>Item` (из `names`);
   finder с `action: "delete"` возвращает `(int64, error)`; поле `body` во входе
   → сырое тело (уже есть `W_RAW_BODY_IMPLICIT` — образец правильного подхода);
   пока блок не переведён в сырую строку (P5.1) — `\\n` вместо `\n` внутри
   `"""`. Шаблон `AGENTS.md` в `ang init`
   должен ссылаться на этот файл.
   > **Сделано (2026-09-15).** Шаг сборки `Conventions Doc` пишет
   > `docs/ang/conventions.md` (путь `docs/ang` добавлен во владение транзакции
   > сборки): правила (таблицы `<entity>s`, имена типов элементов массивов,
   > delete-finder `(int64, error)`, `body` → сырое тело, Go в сырых строках) и
   > таблицы из IR — сущности → таблицы (как `EmitSQL`), поля-массивы операций →
   > типы `port.*`, delete-finders. На dealingi-back — 330 строк, есть `countrys`,
   > `BulkStockReceiveRequestLinesItem`, `AuditLogRepository.DeleteOlderThan`.
   > Шаблона `AGENTS.md` в `ang init` нет — ссылаться пока неоткуда.
5. Каталог действий: добавить `Example` в `flowir.ActionSpec` и **тест, что каждый
   пример декодируется собственным `Decode`** — тогда пример не может быть
   неверным. Примеры брать из матричных тестов
   (`service_flow_codegen_action_matrix_test.go`) — они уже реальные. `ang actions
   --json` печатает их вместо заглушек `<expr>`; `ang explain action repo.Query` —
   тот же ответ для одного действия. Это правка и в `ang-ir` (поле в каталоге), и
   здесь.
   > **Сделано (2026-09-15).** `compiler/flowir/examples.go` — пример для каждого из
   > 244 действий: 27 из `cue/GOLDEN_EXAMPLES.cue`, 127 из тестов, 69 из
   > `cue/GOLDEN_ACTIONS_REFERENCE.cue`, 21 написан по декодерам (у справочного
   > файла многие шаги не декодируются: `db.*` без `source`, `ratelimit.Check` без
   > `rps` — он сгенерирован из заглушек). `TestActionExamplesDecode` декодирует
   > каждый собственным `Decode`. `ang actions` печатает их вместо `<expr>`
   > (осталось 0 заглушек), `ang explain action <name>` — одно действие, hover
   > в LSP — тот же пример. Примеры живут в ANG (flowir), не в ang-ir: там только
   > каталог семантики.

**Приёмка.** Тест на каждый пункт. На dealingi-back: `ang actions --json | jq
'.[]|select(.name=="repo.Query").example'` совпадает по форме с реальным
использованием в `impl_tender_get.cue`; `docs/ang/conventions.md` появился в
`--dry-run` как новый файл и содержит `countrys`.

### P5.6 Пол цикла: 10 с → 3 с

**Проблема.** После P2.2 цена любой проверки — 1,6 с CUE+IR + 8,1 с эмиттеров
(P5.0). Из 8,1 с 5,0 с — `Service Impls`, и почти всё это `formatGoStrict` на 741
файле: чистая процессорная работа, файлы независимы.

**Что сделать.**
1. В `EmitServiceImpl` рендерить и форматировать файлы методов параллельно
   (`errgroup`, лимит `runtime.GOMAXPROCS(0)`); запись — по-прежнему
   `writeFileAtomic`, порядок записи не важен. Осторожно с изменяемыми полями
   `Emitter` (`MissingImpls`, `missingImplIndex`, `e.auditMissingImplementations`) —
   собирать под мьютексом или до параллельной части. Первый вызов
   `imports.Process` прогревает кэш `gopathwalk` — прогрев сделать один раз до
   параллельного участка, иначе N горутин обойдут кэш модулей одновременно.
2. То же для `Port Mocks`, `Postgres Repos`, `Repository Mocks` (по 0,5–0,9 с).
3. Измерить 4,4 с до эмиттеров в настоящей сборке (снимок + `CreateWorkspace`):
   если это копирование, а не символьные ссылки, — оно должно стать ссылками.
4. `go verify` 17 с: проверить, что кэш сборки Go переживает рабочий каталог
   (`GOCACHE` не меняется, пути стабильны); если каждый раз холодный — вот и
   причина.

**Что не делать.** Кеш IR или файлов (прежний P2.2) — не раньше, чем после этого
пункта, и только если после него цикл всё ещё > 3 с. Параллелить *эмиттеры между
собой* — нет: они делят `Emitter` и порядок диагностик.

**Приёмка.** `--check` на dealingi-back: эмиттеры ≤ 3 с при 8 ядрах, выход
байт-в-байт тот же (`--check` чист); `go test -race ./compiler/emitter/`
зелёный; `vet logic --types` ≤ 5 с.

### P5.7 Раскладка CUE (задача проекта, не ANG)

> **Сделано (2026-09-15).** Перед этим пришлось сделать `ang fmt` идемпотентным
> (`e1589c3`): один проход оставлял 11 файлов на второй — `cue fmt` меняет
> отступы строк, и 16 Go-фрагментов, пропущенных из-за числа строк, становились
> форматируемыми; теперь каждый файл форматируется до неподвижной точки.
> dealingi-back: `563271da` лок, `149dd18b` — `ang fmt` (610 файлов, 81
> фрагмент Go; все 615 изменённых сгенерированных файлов совпадают с
> предыдущим коммитом, если убрать строки позиций и sha источников),
> `f098bf70` — шаг `bin/ang fmt --check` в джобе CI `generated-output`.
> Проверять перегенерацию надо сравнением файлов без строк позиций, а не
> фильтром строк диффа: сдвинутые соседние строки выглядят как изменения.

`cue fmt` изменил бы 589 из 646 файлов dealingi-back, у 24 — число строк (а с ним
`// Source:`-комментарии и провенанс в генерате). Один коммит: `ang fmt`, затем
`make generate`, затем в CI-джобу `generated-output` добавить `ang fmt --check`.
Делать **после** P5.4 (иначе коммит утонет в хешах) и **до** P5.1 (перевод в сырые строки
и gofmt блоков правят CUE; на неотформатированном дереве их дифф утонет в
раскладке).

### Порядок выполнения и решения пользователя

**Решения, принятые 2026-09-15.**
1. Go остаётся в CUE; `internal/` целиком генерируется и руками не правится —
   ни `.gen.go`, ни соседние файлы; изменения генерата — только через генератор и
   шаблоны (P5.1).
2. Массовые перегенерирующие коммиты в dealingi-back (без глобального хеша,
   `cue fmt`, сырые строки, разрезка файлов) агент делает сам, каждый — отдельным
   коммитом с проверкой, что дифф генерата состоит только из ожидаемых строк.
3. Предупреждения исправлять, а не подавлять (P5.3). События без потребителя
   (`TenderStock*`, `SearchReindexProgressChanged`) удалить.

**Порядок.** P5.5.1 (баг `l.` → `slog.`) → P5.3 (исправления проверок в ANG и
ang-ir, исправления CUE, вывод сборки) → P5.4 (без глобального хеша) → P5.7
(`cue fmt` проекта) → P5.1.1–2 (сырые строки и полный gofmt блоков) → P5.2
(`//line`) → P5.5 (остальное) → P5.6 (параллельные эмиттеры) → P5.1.3–4 (ошибки
типов и hover в редакторе). Всё, что массово переписывает CUE, идёт после P5.4,
иначе дифф утонет в строках хешей.

---

## Что нельзя менять ни в одном из пунктов

- **Именование в схеме** (`countrys`, `companys`, `b2bsharelinks` и т. п.). Это
  таблицы в боевой базе; изменение требует миграций данных. Не трогать.
- **Форму генерируемого кода и контракт API.** Пока `ang.lock` эталонного проекта
  указывает на старый коммит, джоба `generated-output` сравнивает с ним. Если твоё
  изменение по необходимости меняет выход (например, новая секция в
  `ang-manifest.json`), это должен быть отдельный, объяснённый коммит; проект
  обновит `ang.lock` осознанно.
- **`ensureSDKTarget` и его тесты.**
- Не переписывай `build_transaction.go` «с нуля красиво» — меняй модель владения
  (P0.1), сохраняя rename-атомарность и откат.

## Как проверять любой пункт на эталонном проекте

```bash
cd ~/Desktop/work/ang && git checkout -b <ветка> && go test ./cmd/ang/... ./compiler/...
cd ~/Desktop/work/deal/dealingi-back
git status --short            # должно быть чисто до начала
ANG_SRC=~/Desktop/work/ang make build-ang   # verify-ang упадёт из-за несовпадения lock —
                                            # на время проверки временно поставь commit= в ang.lock
                                            # на HEAD своей ветки, НЕ коммить это
make generate && git status --short         # ожидаемые изменения — только объяснимые
make generate && git status --short         # второй раз: пусто
bin/ang build --check                       # после P1: код 0
git checkout ang.lock                       # вернуть пин
```

Если после твоих изменений `make generate` меняет что-то в `internal/`, `sdk/` или
`api/`, чего ты не ожидал, — это регрессия, а не «побочный эффект». Остановись и
разберись, прежде чем идти дальше.

## Критерий готовности всего плана

Агент, работающий в `dealingi-back`, может: править `impl_*.cue`, за секунды
получить ошибки типов с координатами в CUE (`P3.2`), увидеть дифф будущего кода без
записи (`P2.1`), собрать инкрементально (`P2.2`) — и при этом ни один его файл,
записанный во время сборки, не пропадёт (`P0.1`), фронт не будет удалён (`P0.2`),
а чужой бинарник не сгенерирует другой контракт (`P0.3`). Повторная сборка
даёт ноль диффа, и это проверяет `--check` (`P1`).
