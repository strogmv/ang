// AUTO-GENERATED by ANG
import { apiClient, validateResponse, type ApiRequestOptions } from '../api-client';
import * as Types from '../types';

type ParamRecord = Record<string, unknown>;

const toParamRecord = (params: unknown): ParamRecord =>
  params && typeof params === 'object' ? (params as ParamRecord) : {};

const getPathParamValue = (params: ParamRecord, primary: string, aliases: string[] = []) => {
  const candidates = [primary, ...aliases];
  for (const key of candidates) {
    const value = params[key];
    if (value === undefined || value === null) {
      continue;
    }
    const text = String(value).trim();
    if (text !== '') {
      return text;
    }
  }
  throw new Error(`Missing required path param: ${primary}`);
};

const omitKeys = (params: ParamRecord, keys: string[]): ParamRecord => {
  const next: ParamRecord = { ...params };
  for (const key of keys) {
    delete next[key];
  }
  return next;
};
export const login = (
  params: Types.LoginRequest = {} as Types.LoginRequest,
  requestOptions: ApiRequestOptions = {}
) => {
  return apiClient.post<Types.LoginResponse>(`/auth/login`, params, { signal: requestOptions.signal }).then((r) => {
    const data = r.data;
    if (import.meta.env.DEV) {
      validateResponse('LoginResponseSchema', data, 'Login');
    }
    return data;
  });
};
export const getProfile = (
  params: Types.GetProfileRequest = {} as Types.GetProfileRequest,
  requestOptions: ApiRequestOptions = {}
) => {
  const queryParams = omitKeys(toParamRecord(params), []);
  return apiClient.get<Types.GetProfileResponse>(`/auth/profile`, { params: queryParams, signal: requestOptions.signal }).then((r) => {
    const data = r.data;
    if (import.meta.env.DEV) {
      validateResponse('GetProfileResponseSchema', data, 'GetProfile');
    }
    return data;
  });
};
export const updateProfile = (
  params: Types.UpdateProfileRequest = {} as Types.UpdateProfileRequest,
  requestOptions: ApiRequestOptions = {}
) => {
  return apiClient.put<Types.UpdateProfileResponse>(`/auth/profile`, params, { signal: requestOptions.signal }).then((r) => {
    const data = r.data;
    if (import.meta.env.DEV) {
      validateResponse('UpdateProfileResponseSchema', data, 'UpdateProfile');
    }
    return data;
  });
};
export const register = (
  params: Types.RegisterRequest = {} as Types.RegisterRequest,
  requestOptions: ApiRequestOptions = {}
) => {
  return apiClient.post<Types.RegisterResponse>(`/auth/register`, params, { signal: requestOptions.signal }).then((r) => {
    const data = r.data;
    if (import.meta.env.DEV) {
      validateResponse('RegisterResponseSchema', data, 'Register');
    }
    return data;
  });
};
