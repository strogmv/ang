// Generated by ANG. Do not edit.
// Per-resource Suspense wrappers — pair with error-boundaries.tsx for full fault isolation.
// Usage: <UsersSuspense fallback={<Skeleton />}><UsersList /></UsersSuspense>
'use client';
import React, { Suspense } from 'react';

type SuspenseBoundaryProps = {
  children: React.ReactNode;
  fallback?: React.ReactNode;
};

/** Default skeleton fallback — replace via fallback prop. */
const DefaultFallback = () => (
  <div aria-busy="true" aria-label="Loading..." />
);

/** Suspense boundary for Assistant service data. Renders fallback while queries are loading. */
export const AssistantSuspense = ({ children, fallback }: SuspenseBoundaryProps) => (
  <Suspense fallback={fallback ?? <DefaultFallback />}>{children}</Suspense>
);

/** Suspense boundary for Auth service data. Renders fallback while queries are loading. */
export const AuthSuspense = ({ children, fallback }: SuspenseBoundaryProps) => (
  <Suspense fallback={fallback ?? <DefaultFallback />}>{children}</Suspense>
);

/** Suspense boundary for Blog service data. Renders fallback while queries are loading. */
export const BlogSuspense = ({ children, fallback }: SuspenseBoundaryProps) => (
  <Suspense fallback={fallback ?? <DefaultFallback />}>{children}</Suspense>
);

/** Suspense boundary for Notifications service data. Renders fallback while queries are loading. */
export const NotificationsSuspense = ({ children, fallback }: SuspenseBoundaryProps) => (
  <Suspense fallback={fallback ?? <DefaultFallback />}>{children}</Suspense>
);

/** Wraps children in both a Suspense boundary and an error boundary for a given resource.
 *  Import ErrorBoundary from error-boundaries.tsx to compose full fault isolation:
 *
 *  <UsersErrorBoundary>
 *    <UsersSuspense fallback={<Skeleton />}>
 *      <UsersList />
 *    </UsersSuspense>
 *  </UsersErrorBoundary>
 */
export const AnySuspenseBoundary = ({ children, fallback }: SuspenseBoundaryProps) => (
  <Suspense fallback={fallback ?? <DefaultFallback />}>{children}</Suspense>
);
