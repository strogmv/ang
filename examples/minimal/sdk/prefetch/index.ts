// AUTO-GENERATED by ANG
import type { QueryClient } from '@tanstack/react-query';
import type { LoaderFunctionArgs } from 'react-router-dom';
import * as Options from '../query-options';
import type * as Types from '../types';
export const prefetchUsersList = (queryClient: QueryClient, filters?: Types.ListUsersRequest) =>
  queryClient.prefetchQuery(Options.usersOptions.list(filters));

export const createUsersListLoader = (queryClient: QueryClient) =>
  async ({ request }: LoaderFunctionArgs) => {
    const url = new URL(request.url);
    const filters = Object.fromEntries(url.searchParams) as Types.ListUsersRequest;
    await prefetchUsersList(queryClient, filters);
    return null;
  };
