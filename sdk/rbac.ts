// Generated by ANG. Do not edit.

export type Role = keyof typeof Roles;
export type Permission = keyof typeof Permissions;

export const Roles = {
} as const;

export const Permissions = {
} as const;

export function hasPermission(role: string, permission: string): boolean {
  const perms = (Roles as Record<string, readonly string[]>)[role];
  if (!perms) {
    return false;
  }
  return perms.includes("*") || perms.includes(permission);
}

export function hasAnyPermission(role: string, permissions: readonly string[]): boolean {
  for (const perm of permissions) {
    if (hasPermission(role, perm)) {
      return true;
    }
  }
  return false;
}

export function hasAllPermissions(role: string, permissions: readonly string[]): boolean {
  for (const perm of permissions) {
    if (!hasPermission(role, perm)) {
      return false;
    }
  }
  return true;
}
