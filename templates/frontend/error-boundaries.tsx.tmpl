// Generated by ANG. Do not edit.
// Per-resource React Error Boundaries — wrap each resource's UI in an isolated error boundary.
// Requires react-error-boundary: npm install react-error-boundary
'use client';
import React from 'react';
import { ErrorBoundary, type FallbackProps } from 'react-error-boundary';
import { useQueryErrorResetBoundary } from '@tanstack/react-query';

/** Default error fallback — shows message + retry button. Replace via fallbackComponent prop. */
const DefaultFallback = ({ error, resetErrorBoundary }: FallbackProps) => (
  <div role="alert" style={{ padding: '1rem', border: '1px solid #f87171', borderRadius: 8 }}>
    <p style={{ fontWeight: 600, color: '#dc2626' }}>Something went wrong</p>
    <pre style={{ fontSize: '0.75rem', color: '#6b7280', whiteSpace: 'pre-wrap' }}>
      {error?.message ?? String(error)}
    </pre>
    <button onClick={resetErrorBoundary} style={{ marginTop: '0.5rem' }}>
      Try again
    </button>
  </div>
);

type ResourceBoundaryProps = {
  children: React.ReactNode;
  fallback?: React.ComponentType<FallbackProps>;
  onError?: (error: Error, info: React.ErrorInfo) => void;
};

{{- range .QueryResources }}

/** Error boundary isolating {{ .key | Title }} data UI. Resets on query-client reset. */
export const {{ .key | Title }}ErrorBoundary = ({
  children,
  fallback: FallbackComponent = DefaultFallback,
  onError,
}: ResourceBoundaryProps) => {
  const { reset } = useQueryErrorResetBoundary();
  return (
    <ErrorBoundary
      onReset={reset}
      FallbackComponent={FallbackComponent}
      onError={onError}
    >
      {children}
    </ErrorBoundary>
  );
};
{{- end }}

/** Catches errors from any ANG resource query. Use at page or layout level. */
export const AnyResourceErrorBoundary = ({
  children,
  fallback: FallbackComponent = DefaultFallback,
  onError,
}: ResourceBoundaryProps) => {
  const { reset } = useQueryErrorResetBoundary();
  return (
    <ErrorBoundary
      onReset={reset}
      FallbackComponent={FallbackComponent}
      onError={onError}
    >
      {children}
    </ErrorBoundary>
  );
};
