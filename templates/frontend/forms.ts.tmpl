// Generated by ANG. Do not edit.
import { useForm, UseFormProps } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import type { z } from 'zod';
import * as Schemas from '../schemas';

{{- range .Endpoints }}
{{- if and (ne .Method "GET") (ne .Method "WS") }}
{{- $reqName := printf "%sRequest" .RPC }}
// Form values are the schema's input (a date field may still be a string);
// submit handlers receive its parsed output.
export const use{{ .RPC }}Form = (
  props?: UseFormProps<z.input<typeof Schemas.{{ $reqName }}Schema>, unknown, z.output<typeof Schemas.{{ $reqName }}Schema>>
) => {
  return useForm<z.input<typeof Schemas.{{ $reqName }}Schema>, unknown, z.output<typeof Schemas.{{ $reqName }}Schema>>({
    resolver: zodResolver(Schemas.{{ $reqName }}Schema),
    ...props,
  });
};
{{- end }}
{{- end }}
