-- Auto-generated by ANG. Do not edit manually.

CREATE TABLE IF NOT EXISTS {{ .Name | ToLower }}s (
{{- $first := true }}
{{- range $i, $f := .Fields }}
{{- if not $f.SkipDomain }}
{{- if not $first }},{{ end }}
    "{{ $f.Name | DBName }}" {{ SQLType $f }}{{ if $f.Default }} DEFAULT {{ SQLDefault $f.Default }}{{ end }}{{ if $f.DB.PrimaryKey }} PRIMARY KEY{{ end }}{{ if $f.DB.Unique }} UNIQUE{{ end }}{{ if not $f.IsOptional }} NOT NULL{{ end }}
{{- $first = false }}
{{- end }}
{{- end }}
);
