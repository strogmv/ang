// Generated by ANG. Do not edit.
import { queryOptions, infiniteQueryOptions } from '@tanstack/react-query';
import * as Types from '../types';
import { queryKeys } from '../query-keys';
import * as api from '../endpoints';

{{- range .Endpoints }}
{{- if eq .Method "GET" }}
export const {{ LowerFirst .RPC }}QueryOptions = ({{ RequestParam .RPC }}) => {
  return queryOptions({
    queryKey: queryKeys.endpoint.{{ .ServiceName }}.{{ .RPC }}(params),
    queryFn: async ({ signal }) => {
      return api.{{ LowerFirst .RPC }}(params, { signal });
    },
  });
};

{{- if .Pagination }}
export const {{ LowerFirst .RPC }}InfiniteQueryOptions = ({{ RequestParam .RPC }}) => {
  return infiniteQueryOptions({
    queryKey: queryKeys.endpoint.{{ .ServiceName }}.{{ .RPC }}(params),
    queryFn: async ({ pageParam, signal }: { pageParam: {{ if eq .Pagination.Type "offset" }}number{{ else }}string | null{{ end }}; signal: AbortSignal }) => {
      const p: Record<string, unknown> = { ...((params ?? {}) as Record<string, unknown>) };
      {{- if eq .Pagination.Type "offset" }}
      p.offset = pageParam;
      p.limit = p.limit ?? {{ if .Pagination.DefaultLimit }}{{ .Pagination.DefaultLimit }}{{ else }}20{{ end }};
      {{- else }}
      p.cursor = pageParam;
      {{- end }}
      return api.{{ LowerFirst .RPC }}(p as unknown as Types.{{ .RPC }}Request, { signal });
    },
    initialPageParam: {{ if eq .Pagination.Type "offset" }}0{{ else }}null{{ end }},
    getNextPageParam: (lastPage: Types.{{ .RPC }}Response, allPages: Types.{{ .RPC }}Response[]) => {
      {{- if eq .Pagination.Type "offset" }}
      {{- $list := PageListField .RPC }}
      {{- if $list }}
      if (!lastPage.{{ $list }} || lastPage.{{ $list }}.length < (params.limit ?? {{ if .Pagination.DefaultLimit }}{{ .Pagination.DefaultLimit }}{{ else }}20{{ end }})) {
        return undefined;
      }
      return allPages.length * (params.limit ?? {{ if .Pagination.DefaultLimit }}{{ .Pagination.DefaultLimit }}{{ else }}20{{ end }});
      {{- else }}
      void lastPage;
      void allPages;
      return undefined;
      {{- end }}
      {{- else }}
      return (lastPage as { nextCursor?: string | null }).nextCursor || undefined;
      {{- end }}
    },
  });
};
{{- end }}
{{- end }}
{{- end }}
