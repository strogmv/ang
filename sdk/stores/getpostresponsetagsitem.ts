// Generated by ANG. Do not edit.
import { create } from 'zustand';
import type * as Types from '../types';
import { registerStoreInvalidator } from './invalidation';
type GetPostResponseTagsItemId = Types.GetPostResponseTagsItem['id'];

const getPostResponseTagsItemIdKey = (item: Types.GetPostResponseTagsItem): string =>
  String(item.id);

export interface GetPostResponseTagsItemStore {
  items: Record<string, Types.GetPostResponseTagsItem>;
  list: Types.GetPostResponseTagsItem[];
  listStale: boolean;
  listVersion: number;
  setAll: (items: Types.GetPostResponseTagsItem[]) => void;
  upsert: (item: Types.GetPostResponseTagsItem) => void;
  remove: (id: GetPostResponseTagsItemId) => void;
  markListStale: () => void;
  clearListStale: () => void;
  clear: () => void;
}

export const useGetPostResponseTagsItemStore = create<GetPostResponseTagsItemStore>()((set) => ({
  items: {},
  list: [],
  listStale: false,
  listVersion: 0,
  setAll: (items) => {
    const index: Record<string, Types.GetPostResponseTagsItem> = {};
    for (const item of items) {
      index[getPostResponseTagsItemIdKey(item)] = item;
    }
    set((state) => ({ items: index, list: items, listStale: false, listVersion: state.listVersion + 1 }));
  },
  upsert: (item) =>
    set((state) => {
      const key = getPostResponseTagsItemIdKey(item);
      const items = { ...state.items, [key]: item };
      const exists = state.list.some((entry) => getPostResponseTagsItemIdKey(entry) === key);
      const list = exists
        ? state.list.map((entry) => (getPostResponseTagsItemIdKey(entry) === key ? item : entry))
        : [item, ...state.list];
      return { items, list, listStale: false, listVersion: state.listVersion + 1 };
    }),
  remove: (id) =>
    set((state) => {
      const key = String(id);
      if (!state.items[key]) {
        return state;
      }
      const items = { ...state.items };
      delete items[key];
      const list = state.list.filter((entry) => getPostResponseTagsItemIdKey(entry) !== key);
      return { items, list, listStale: false, listVersion: state.listVersion + 1 };
    }),
  markListStale: () =>
    set((state) => {
      if (state.listStale) return state;
      return { listStale: true, listVersion: state.listVersion + 1 };
    }),
  clearListStale: () => set({ listStale: false }),
  clear: () => set((state) => ({ items: {}, list: [], listStale: false, listVersion: state.listVersion + 1 })),
}));

registerStoreInvalidator('getpostresponsetagsitem', () => {
  useGetPostResponseTagsItemStore.getState().markListStale();
});
