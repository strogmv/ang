// Generated by ANG. Do not edit.
import { queryOptions, infiniteQueryOptions } from '@tanstack/react-query';
import * as Types from '../types';
import { queryKeys } from '../query-keys';
import * as api from '../endpoints';
export const listUsersQueryOptions = (params: Types.ListUsersRequest = {} as Types.ListUsersRequest) => {
  return queryOptions({
    queryKey: queryKeys.users.list(params),
    queryFn: async () => {
      return api.listUsers(params);
    },
  });
};
export const listUsersInfiniteQueryOptions = (params: Types.ListUsersRequest = {} as Types.ListUsersRequest) => {
  return infiniteQueryOptions({
    queryKey: queryKeys.users.list(params),
    queryFn: async ({ pageParam }: { pageParam: any }) => {
      const p = { ...(params as any) };
      p.offset = pageParam;
      p.limit = p.limit ?? 20;
      return api.listUsers(p);
    },
    initialPageParam: 0,
    getNextPageParam: (lastPage: any, allPages: any[]) => {
      if (!lastPage.data || lastPage.data.length < (params.limit ?? 20)) {
        return undefined;
      }
      return allPages.length * (params.limit ?? 20);
    },
  });
};
