// AUTO-GENERATED by ANG
import { queryOptions } from '@tanstack/react-query';
import { queryKeys } from './query-keys';
import * as api from './endpoints';
{{- if or .QueryOptionsNeedsTypes HasGETEndpoints }}
import type * as Types from './types';
{{- end }}

export type QueryProfileOptions = {
  staleTime?: number;
  gcTime?: number;
  refetchOnMount?: 'always' | boolean;
  refetchOnWindowFocus?: boolean;
  refetchOnReconnect?: boolean;
  retry?: number | boolean;
};

export const queryProfiles: Record<string, QueryProfileOptions> = {
  realtime: {
    staleTime: 0,
    gcTime: 0,
    refetchOnMount: 'always',
    refetchOnWindowFocus: true,
    refetchOnReconnect: true,
    retry: 1,
  },
};

// Canonical React Query options for every GET RPC (single key-space: queryKeys.endpoint.*).
export const endpointQueryOptions = {
{{- range .Endpoints }}
{{- if eq .Method "GET" }}
{{- $profile := EndpointQueryProfile . }}
  {{ LowerFirst .RPC }}: (params: Types.{{ .RPC }}Request = {} as Types.{{ .RPC }}Request) =>
    queryOptions({
      queryKey: queryKeys.endpoint.{{ .ServiceName }}.{{ .RPC }}(params),
      queryFn: () => api.{{ LowerFirst .RPC }}(params),
      {{- if $profile }}
      ...(queryProfiles['{{ $profile }}'] || {}),
      {{- else }}
      {{- if .CacheTTL }}
      staleTime: {{ CacheTTLToMs .CacheTTL }},
      {{- end }}
      {{- end }}
    }),

{{- end }}
{{- end }}
};
