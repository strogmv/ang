// AUTO-GENERATED by ANG
/**
 * DTO → view-model mappers for the UI layer.
 * ANG does not infer field renames from backend; add explicit functions here
 * (or in sibling modules) and import them from app code.
 *
 * Example:
 *   export function mapUserDtoToProfile(dto: Types.GetMeResponse): UserProfile { ... }
 */
export {};
