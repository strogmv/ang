// Generated by ANG. Do not edit.
import { create } from 'zustand';
import type * as Types from '../types';
type PostId = Types.Post['id'];

const postIdKey = (item: Types.Post): string =>
  String(item.id);

export interface PostStore {
  items: Record<string, Types.Post>;
  list: Types.Post[];
  setAll: (items: Types.Post[]) => void;
  upsert: (item: Types.Post) => void;
  remove: (id: PostId) => void;
  clear: () => void;
}

export const usePostStore = create<PostStore>()((set) => ({
  items: {},
  list: [],
  setAll: (items) => {
    const index: Record<string, Types.Post> = {};
    for (const item of items) {
      index[postIdKey(item)] = item;
    }
    set({ items: index, list: items });
  },
  upsert: (item) =>
    set((state) => {
      const key = postIdKey(item);
      const items = { ...state.items, [key]: item };
      const exists = state.list.some((entry) => postIdKey(entry) === key);
      const list = exists
        ? state.list.map((entry) => (postIdKey(entry) === key ? item : entry))
        : [item, ...state.list];
      return { items, list };
    }),
  remove: (id) =>
    set((state) => {
      const key = String(id);
      if (!state.items[key]) {
        return state;
      }
      const items = { ...state.items };
      delete items[key];
      const list = state.list.filter((entry) => postIdKey(entry) !== key);
      return { items, list };
    }),
  clear: () => set({ items: {}, list: [] }),
}));
