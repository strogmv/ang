// Generated by ANG. Do not edit.
import {
  useQuery,
  useMutation,
  useQueryClient,
  useInfiniteQuery,
  useSuspenseQuery,
  useSuspenseInfiniteQuery,
  UseQueryOptions,
  UseMutationOptions,
  UseSuspenseQueryOptions,
  UseSuspenseInfiniteQueryOptions,
  type InfiniteData,
} from '@tanstack/react-query';
import * as Types from '../types';
import * as QueryOptions from '../query-options';
import * as LegacyQueries from '../queries';
import * as api from '../endpoints';
import { endpointMeta } from '../endpoints';
import { appWsClient, useWebsocketSubscription, useAutoInvalidation, useAppWebsocketSubscription } from './websocket-hooks';
import { WebSocketClient, getWebSocketBaseUrl } from '../websocket';
import { queryKeys } from '../query-keys';
import { useAuthStore } from '../auth-store';
import { useEffect, useRef, useLayoutEffect } from 'react';

export { appWsClient, useWebsocketSubscription, useAppWebsocketSubscription, useAutoInvalidation };


type MutationInvalidateTarget = {
  store: string;
  service?: string;
  rpc?: string;
  scopeParam?: string;
  mode?: 'list' | 'detail' | 'aggregate';
};

type GeneratedMutationContext = {
  queryKey?: readonly unknown[];
  previousData?: unknown;
};

type AuthMutationPayload = {
  accessToken: string;
  refreshToken?: string;
};

const invalidateGeneratedTargets = (
  queryClient: ReturnType<typeof useQueryClient>,
  mutationName: string,
  variables: Record<string, unknown>,
) => {
  const targets = (endpointMeta as Record<string, { invalidateTargets?: MutationInvalidateTarget[] }>)[mutationName]?.invalidateTargets || [];
  for (const target of targets) {
    const storeName = String(target.store || '').toLowerCase();
    const serviceName = String(target.service || '');
    const rpcName = String(target.rpc || '');
    const scopeParam = target.scopeParam;
    const scopeValue = scopeParam ? variables[scopeParam] : undefined;

    if (serviceName && rpcName) {
      queryClient.invalidateQueries({
        predicate: (query) => {
          const key = query.queryKey;
          if (!Array.isArray(key) || key.length < 2) return false;
          if (String(key[0] || '') !== serviceName) return false;
          if (String(key[1] || '') !== rpcName) return false;
          if (target.mode === 'detail' && scopeParam && scopeValue !== undefined && scopeValue !== null && scopeValue !== '') {
            const params = key[2];
            return Boolean(params && typeof params === 'object' && (params as Record<string, unknown>)[scopeParam] === scopeValue);
          }
          return true;
        },
      });
      continue;
    }

    if (target.mode === 'detail' && scopeParam && scopeValue !== undefined && scopeValue !== null && scopeValue !== '') {
      queryClient.invalidateQueries({
        predicate: (query) => {
          const key = query.queryKey;
          if (!Array.isArray(key) || key.length < 3) return false;
          if (String(key[0] || '').toLowerCase() !== storeName) return false;
          const params = key[2];
          return Boolean(params && typeof params === 'object' && (params as Record<string, unknown>)[scopeParam] === scopeValue);
        },
      });
      continue;
    }

    queryClient.invalidateQueries({
      predicate: (query) => {
        const key = query.queryKey;
        return Array.isArray(key) && key.length > 0 && String(key[0] || '').toLowerCase() === storeName;
      },
    });
  }
};

const missingRequiredParamsError = (hookName: string) => new Error(`${hookName} requires all path params before suspense execution`);

{{- range .Endpoints }}
{{- if eq .Method "GET" }}
{{- $ep := . }}
{{- $argList := PathArgNames .Path }}
{{- $optRes := QueryOptionsResource .RPC }}
{{- $optKind := QueryOptionsKind .RPC }}
{{- $listPref := DetailPlaceholderListPrefixTuple .RPC }}
export const use{{ $ep.RPC }} = (params?: Types.{{ $ep.RPC }}Request, options?: Omit<UseQueryOptions<Types.{{ $ep.RPC }}Response, Error, Types.{{ $ep.RPC }}Response, readonly unknown[]>, 'queryKey' | 'queryFn'>) => {
  const hasRequiredParams = {{- if gt (len $argList) 0 -}} !!({{- range $i, $a := $argList -}}{{ if $i }} && {{ end }}((params as Types.{{ $ep.RPC }}Request | undefined)?.{{ JSONName $a }}){{- end -}}) {{- else -}} true {{- end }};
  {{- if and $optRes (eq $optKind "detail") (ne $listPref "") }}
  const queryClient = useQueryClient();
  {{- end }}
  return useQuery<Types.{{ $ep.RPC }}Response, Error>({
    {{- if and $optRes (eq $optKind "detail") (ne $listPref "") }}
    placeholderData: () => {
      const id = ((params as Types.{{ $ep.RPC }}Request | undefined)?.{{ QueryOptionsDetailParam $ep.RPC }});
      if (!id) return undefined;
      const cachedLists = queryClient.getQueriesData<Types.{{ $ep.RPC }}Response>({
        queryKey: {{ $listPref }},
      });
      for (const [, listData] of cachedLists) {
        const found = (listData as { data?: Record<string, unknown>[] } | undefined)?.data?.find(
          (item: Record<string, unknown>) => item['{{ QueryOptionsDetailParam $ep.RPC }}'] === id,
        );
        if (found) return { data: found } as unknown as Types.{{ $ep.RPC }}Response;
      }
      return undefined;
    },
    {{- end }}
    ...QueryOptions.endpointQueryOptions.{{ LowerFirst $ep.RPC }}((params ?? {}) as Types.{{ $ep.RPC }}Request),
    ...options,
    enabled: hasRequiredParams && (options?.enabled ?? true),
  });
};

export const use{{ $ep.RPC }}Suspense = (
  params?: Types.{{ $ep.RPC }}Request,
  options?: Omit<
    UseSuspenseQueryOptions<Types.{{ $ep.RPC }}Response, Error, Types.{{ $ep.RPC }}Response, readonly unknown[]>,
    'queryKey' | 'queryFn'
  >,
) => {
  const hasRequiredParams = {{- if gt (len $argList) 0 -}} !!({{- range $i, $a := $argList -}}{{ if $i }} && {{ end }}((params as Types.{{ $ep.RPC }}Request | undefined)?.{{ JSONName $a }}){{- end -}}) {{- else -}} true {{- end }};
  const baseOptions = QueryOptions.endpointQueryOptions.{{ LowerFirst $ep.RPC }}((params ?? {}) as Types.{{ $ep.RPC }}Request);
  if (!hasRequiredParams) {
    throw missingRequiredParamsError('use{{ $ep.RPC }}Suspense');
  }
  return useSuspenseQuery<Types.{{ $ep.RPC }}Response, Error>({
    ...baseOptions,
    ...options,
  });
};

{{- if $ep.Pagination }}
export const useInfinite{{ $ep.RPC }} = (params?: Types.{{ $ep.RPC }}Request, options?: Record<string, unknown>) => {
  const hasRequiredParams = {{- if gt (len $argList) 0 -}} !!({{- range $i, $a := $argList -}}{{ if $i }} && {{ end }}((params as Types.{{ $ep.RPC }}Request | undefined)?.{{ JSONName $a }}){{- end -}}) {{- else -}} true {{- end }};
  {{- if eq $ep.Pagination.Type "offset" }}
  return useInfiniteQuery<Types.{{ $ep.RPC }}Response, Error, InfiniteData<Types.{{ $ep.RPC }}Response>, readonly unknown[], number>({
    ...LegacyQueries.{{ LowerFirst $ep.RPC }}InfiniteQueryOptions((params ?? {}) as Types.{{ $ep.RPC }}Request),
    ...options,
    enabled: hasRequiredParams && ((options as { enabled?: boolean } | undefined)?.enabled ?? true),
  });
  {{- else }}
  return useInfiniteQuery<Types.{{ $ep.RPC }}Response, Error, InfiniteData<Types.{{ $ep.RPC }}Response>, readonly unknown[], string | null>({
    ...LegacyQueries.{{ LowerFirst $ep.RPC }}InfiniteQueryOptions((params ?? {}) as Types.{{ $ep.RPC }}Request),
    ...options,
    enabled: hasRequiredParams && ((options as { enabled?: boolean } | undefined)?.enabled ?? true),
  });
  {{- end }}
};

export const useInfinite{{ $ep.RPC }}Suspense = (
  params?: Types.{{ $ep.RPC }}Request,
  options?: Omit<
    UseSuspenseInfiniteQueryOptions<
      Types.{{ $ep.RPC }}Response,
      Error,
      InfiniteData<Types.{{ $ep.RPC }}Response>,
      readonly unknown[],
      {{- if eq $ep.Pagination.Type "offset" }}number{{- else }}string | null{{- end }}
    >,
    'queryKey' | 'queryFn'
  >,
) => {
  const hasRequiredParams = {{- if gt (len $argList) 0 -}} !!({{- range $i, $a := $argList -}}{{ if $i }} && {{ end }}((params as Types.{{ $ep.RPC }}Request | undefined)?.{{ JSONName $a }}){{- end -}}) {{- else -}} true {{- end }};
  const baseInfinite = LegacyQueries.{{ LowerFirst $ep.RPC }}InfiniteQueryOptions((params ?? {}) as Types.{{ $ep.RPC }}Request);
  {{- if eq $ep.Pagination.Type "offset" }}
  if (!hasRequiredParams) {
    throw missingRequiredParamsError('useInfinite{{ $ep.RPC }}Suspense');
  }
  return useSuspenseInfiniteQuery<Types.{{ $ep.RPC }}Response, Error, InfiniteData<Types.{{ $ep.RPC }}Response>, readonly unknown[], number>({
    ...baseInfinite,
    ...options,
  });
  {{- else }}
  if (!hasRequiredParams) {
    throw missingRequiredParamsError('useInfinite{{ $ep.RPC }}Suspense');
  }
  return useSuspenseInfiniteQuery<Types.{{ $ep.RPC }}Response, Error, InfiniteData<Types.{{ $ep.RPC }}Response>, readonly unknown[], string | null>({
    ...baseInfinite,
    ...options,
  });
  {{- end }}
};
{{- end }}
{{- else if eq .Method "WS" }}
{{- $params := PathParams .Path }}
{{- $args := PathArgs .Path }}
export const use{{ .RPC }}Raw = ({{- if ne $params "" }}{{ $params }}, {{ end }}onMessage: (data: unknown) => void) => {
  // Ref-pattern: stable subscription, always calls the latest callback.
  const onMessageRef = useRef(onMessage);
  useLayoutEffect(() => { onMessageRef.current = onMessage; });

  useEffect(() => {
    {{- if ne $params "" }}
    const wsUrl = getWebSocketBaseUrl() + `{{ PathTemplate .Path }}`;
    const client = new WebSocketClient(wsUrl);
    const unsubscribe = client.subscribe((data: unknown) => onMessageRef.current(data));
    return () => {
      unsubscribe();
      client.close();
    };
    {{- else }}
    const unsubscribe = appWsClient.subscribe((data: unknown) => onMessageRef.current(data));
    return () => unsubscribe();
    {{- end }}
  }, [{{ $args }}]);
};
{{- else }}
{{- $relatedRPC := GetRelatedReadRPC . }}
{{- if ne $relatedRPC "" }}
export const use{{ .RPC }} = (options?: UseMutationOptions<Types.{{ .RPC }}Response, Error, Types.{{ .RPC }}Request, GeneratedMutationContext>) => {
  const queryClient = useQueryClient();
  
  return useMutation({
    mutationFn: async (variables: Types.{{ .RPC }}Request) => {
      return api.{{ LowerFirst .RPC }}(variables);
    },
    // Optimistic Update for {{ $relatedRPC }} (canonical endpoint query keys only)
    onMutate: async (variables: Types.{{ .RPC }}Request) => {
        {{- /* Check if it's a detail update or list update */ -}}
        {{- $isList := or (HasPrefix $relatedRPC "List") (HasPrefix $relatedRPC "AdminList") }}
        {{- $relTuple := InvalidateGETEndpointTuple $relatedRPC }}
        {{- $relSvc := ServiceNameForGETRPC $relatedRPC }}
        const _v = variables as unknown as Record<string, unknown>;
        const id = _v['{{ JSONName "id" }}'] ?? _v['{{ JSONName (GetEntityIDParam .RPC) }}'];
        
        {{- if $isList }}
        // List: cancel by endpoint prefix; do not patch list cache (params vary per query key).
        {{- if $relTuple }}
        const queryKey = {{ $relTuple }};
        await queryClient.cancelQueries({ queryKey });
        return { previousData: undefined, queryKey };
        {{- else }}
        return {};
        {{- end }}

        {{- else }}
        // Detail Update
        {{- if $relSvc }}
        if (!id) return;
        const queryKey = queryKeys.endpoint.{{ $relSvc }}.{{ $relatedRPC }}(variables as Types.{{ $relatedRPC }}Request);
        await queryClient.cancelQueries({ queryKey });
        const previousData = queryClient.getQueryData(queryKey);
        queryClient.setQueryData(queryKey, (old: unknown) => {
            return old && typeof old === 'object' ? { ...(old as object), ...variables } : variables;
        });
        return { previousData, queryKey };
        {{- else }}
        return {};
        {{- end }}
        {{- end }}
    },
    onError: (_err: Error, _variables: Types.{{ .RPC }}Request, context: GeneratedMutationContext | undefined) => {
        if (context?.previousData !== undefined && context?.queryKey) {
            queryClient.setQueryData(context.queryKey, context.previousData);
        }
    },
    onSettled: (_data: unknown, _error: Error | null, _variables: Types.{{ .RPC }}Request, context: GeneratedMutationContext | undefined) => {
        if (context?.queryKey) {
            queryClient.invalidateQueries({ queryKey: context.queryKey });
        }
        invalidateGeneratedTargets(queryClient, '{{ LowerFirst .RPC }}', (_variables ?? {}) as Record<string, unknown>);
    },
    ...options,
  });
};
{{- else }}
export const use{{ .RPC }} = (options?: UseMutationOptions<Types.{{ .RPC }}Response, Error, Types.{{ .RPC }}Request, GeneratedMutationContext>) => {
  {{- if .Invalidate }}
  const queryClient = useQueryClient();
  {{- end }}

  return useMutation({
    mutationFn: async (variables: Types.{{ .RPC }}Request) => {
      return api.{{ LowerFirst .RPC }}(variables);
    },
    {{- if IsAuthLoginRPC .RPC }}
    onSuccess: (data: Types.{{ .RPC }}Response) => {
      const store = useAuthStore.getState();
      const authData = data as AuthMutationPayload;
      store.setAuth(authData.accessToken, store.user, authData.refreshToken ?? undefined);
    },
    {{- end }}
    {{- if .Invalidate }}
    onSettled: (_data: unknown, _error: Error | null, _variables: Types.{{ .RPC }}Request) => {
        invalidateGeneratedTargets(queryClient, '{{ LowerFirst .RPC }}', (_variables ?? {}) as Record<string, unknown>);
    },
    {{- end }}
    ...options,
  });
};
{{- end }}
{{- end }}
{{- end }}
