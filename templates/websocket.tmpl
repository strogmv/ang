// ============================================================================
// AUTO-GENERATED by ANG (Architectural Normalized Generator)
// Version: {{ ANGVersion }}
// CompilerHash: {{ CompilerHash }}
// DO NOT EDIT - changes will be overwritten on next generation
// GENERATED: true
// ============================================================================

package http

import (
	{{- if .HasLive }}
	"context"
	"strconv"
	"time"
	{{- end }}
	"encoding/json"
	"net/http"

	"github.com/go-chi/chi/v5"
	{{- if .HasLive }}
	"github.com/gorilla/websocket"
	{{- end }}
	{{- if or .HasBroadcast .HasLive }}
	"{{GoModule}}/internal/domain"
	{{- end }}
	{{- if .HasAuthCheck }}
	"{{GoModule}}/internal/pkg/errors"
	{{- end }}
	"{{GoModule}}/internal/port"
)

var {{ .Name | ToLower }}Hub = &wsHub{
	rooms:      make(map[string]map[*wsClient]bool),
	broadcast:  make(chan wsMessage, 1024),
	register:   make(chan *wsClient),
	unregister: make(chan *wsClient),
	subscribe:  make(chan wsRoomSubscription),
	unsubscribe: make(chan wsRoomSubscription),
	direct:      make(chan wsDirect, 1024),
	stop:       make(chan struct{}),
}

func init() {
	go {{ .Name | ToLower }}Hub.run()
}

func Stop{{ .Name }}Hub() {
	{{ .Name | ToLower }}Hub.stop <- struct{}{}
}

// Broadcast{{ .Name }}WS sends a message to all connected clients in a room.
func Broadcast{{ .Name }}WS(roomID string, messageType string, payload interface{}) {
	msg := wsBroadcast{Type: messageType, Payload: payload}
	data, err := json.Marshal(msg)
	if err != nil {
		return
	}
	// Never block the caller (the NATS relay): when the hub is saturated the
	// message is dropped. Clients detect the gap from the event versions and
	// resynchronise over REST, which beats stalling every room of the process.
	select {
	case {{ .Name | ToLower }}Hub.broadcast <- wsMessage{room: roomID, data: data}:
	default:
		wsDroppedBroadcasts.Add(1)
	}
}

{{- $uniqueMessages := makeMap }}

{{- range .Endpoints }}

{{- range .Messages }}

{{- if not (mapHas $uniqueMessages .) }}

{{- mapSet $uniqueMessages . true }}

func Broadcast{{ . }}({{ if $.HasRooms }}roomID string, {{ end }}payload domain.{{ ExportName . }}) {

	Broadcast{{ $.Name }}WS({{ if $.HasRooms }}roomID{{ else }}""{{ end }}, "{{ . }}", payload)

}

{{- end }}

{{- end }}

{{- end }}



func Register{{ .Name }}WSRoutes(r chi.Router, svc port.{{ .Name }}) {
{{- range .Endpoints }}
	{{- if .IsLive }}
	// {{ .RPC }} is a live room: see ws_live.go.
	registerLiveEndpoint(&liveEndpoint{
		name: "{{ .LiveName }}",
		hub:  {{ $.Name | ToLower }}Hub,
		build: func(ctx context.Context, room string) (string, error) {
			resp, err := svc.{{ .LiveState }}(ctx, port.{{ .LiveState }}Request{ {{ .LiveStateRoomField }}: room })
			if err != nil {
				return "", err
			}
			return resp.State, nil
		},
		view: func(ctx context.Context, state string, companies []string, now time.Time) ([]string, []string, time.Time, error) {
			resp, err := svc.{{ .LiveView }}(ctx, port.{{ .LiveView }}Request{
				{{ .LiveViewStateField }}:     state,
				{{ .LiveViewCompaniesField }}: companies,
				{{ .LiveViewNowField }}:       now.UTC().Format(time.RFC3339Nano),
			})
			if err != nil {
				return nil, nil, time.Time{}, err
			}
			var refreshAt time.Time
			if resp.{{ .LiveViewRefreshField }} != "" {
				refreshAt, _ = time.Parse(time.RFC3339Nano, resp.{{ .LiveViewRefreshField }})
			}
			return resp.{{ .LiveViewSnapshotsField }}, resp.{{ .LiveViewDeniedField }}, refreshAt, nil
		},
		triggers: map[string]func([]byte) string{
			{{- range .LiveTriggers }}
			"{{ .Event }}": func(data []byte) string {
				var event domain.{{ .GoType }}
				if json.Unmarshal(data, &event) != nil {
					return ""
				}
				return event.{{ .RoomField }}
			},
			{{- end }}
		},
	})
	{{- end }}
	{{- $mw := WSMiddlewareList . }}
	{{- if $mw }}
	r.With({{ $mw }}).
		Get("{{ .Path }}", handler{{ .RPC }}WS(svc))
	{{- else }}
	r.Get("{{ .Path }}", handler{{ .RPC }}WS(svc))
	{{- end }}
{{- end }}
}

{{- range .Endpoints }}
func handler{{ .RPC }}WS(svc port.{{ $.Name }}) http.HandlerFunc {
	return func(w http.ResponseWriter, r *http.Request) {
		{{- $ep := . }}
		{{- $hasParams := false }}
		{{- if .Input.Name }}
		{{- range .Input.Fields }}
		{{- $param := ParamForField $ep.Path .Name }}
		{{- if ne $param "" }}
		{{- $hasParams = true }}
		{{- end }}
		{{- end }}
		{{- end }}
		{{- if $hasParams }}
		var req port.{{ .Input.Name }}
		{{- range .Input.Fields }}
		{{- $param := ParamForField $ep.Path .Name }}
		{{- if ne $param "" }}
		req.{{ .Name | ExportName }} = chi.URLParam(r, "{{ $param }}")
		{{- end }}
		{{- end }}
		_ = req // silence unused warning if no params extracted
		{{- end }}
		{{- $needsUserID := false }}
		{{- if .RoomParam }}
		{{- $roomParam := ParamForField .Path .RoomParam }}
		{{- if and (not $roomParam) (stringsEqualFold .RoomParam "userId") }}
		{{- $needsUserID = true }}
		{{- end }}
		{{- else if HasInputField .Input "userId" }}
		{{- $needsUserID = true }}
		{{- end }}
		{{- if .AuthCheck }}
		// auth.check {{ .AuthCheck }} guards this room. It runs before the
		// upgrade, on the identity the handshake itself carries (a Bearer header,
		// ?token= or the session cookie), so a caller it refuses gets a plain
		// 401/403 and never a socket. The post-upgrade auth frame is not read.
		{{- if or .AuthCheckUserField .AuthCheckCompanyField $needsUserID }}
		wsIdent, wsIdentErr := wsHandshakeIdentity(r)
		{{- else }}
		_, wsIdentErr := wsHandshakeIdentity(r)
		{{- end }}
		if wsIdentErr != nil {
			if !writeSessionFailure(w, r, wsIdentErr) {
				errors.WriteError(w, r, errors.New(http.StatusUnauthorized, "Unauthorized", "Sign in to open this stream"))
			}
			return
		}
		var authReq port.{{ .AuthCheck }}Request
		{{- range .AuthCheckInput.Fields }}
		{{- $param := ParamForField $ep.Path .Name }}
		{{- if ne $param "" }}
		authReq.{{ .Name | ExportName }} = chi.URLParam(r, "{{ $param }}")
		{{- end }}
		{{- end }}
		{{- if .AuthCheckCompanyField }}
		authReq.{{ .AuthCheckCompanyField }} = wsIdent.CompanyID
		{{- end }}
		{{- if .AuthCheckUserField }}
		authReq.{{ .AuthCheckUserField }} = wsIdent.UserID
		{{- end }}
		authResp, err := svc.{{ .AuthCheck }}(r.Context(), authReq)
		if err != nil {
			errors.WriteError(w, r, err)
			return
		}
		if !authResp.Allowed {
			errors.WriteError(w, r, errors.New(http.StatusForbidden, "Forbidden", "Insufficient access"))
			return
		}
		{{- end }}
		conn, err := wsUpgrader.Upgrade(w, r, nil)
		if err != nil {
			return
		}

		{{- if .AuthCheck }}
		{{- if $needsUserID }}
		wsUserID := wsIdent.UserID
		{{- end }}
		{{- else }}

		// Auth happens post-upgrade: client sends { type: "auth", token: "<jwt>" } as first message.
		// This keeps JWT out of the URL (access logs, browser history, Referer headers).
		{{- if $needsUserID }}
		wsUserID, _, wsAuthErr := wsReadAuthTokenOrQuery(conn, r)
		{{- else }}
		_, _, wsAuthErr := wsReadAuthTokenOrQuery(conn, r)
		{{- end }}
		if wsAuthErr != nil {
			return
		}
		{{- end }}

		roomID := ""
		{{- if .RoomParam }}
		{{- $roomParam := ParamForField .Path .RoomParam }}
		{{- if $roomParam }}
		roomID = chi.URLParam(r, "{{ $roomParam }}")
		{{- else if stringsEqualFold .RoomParam "userId" }}
		roomID = wsUserID
		{{- end }}
		{{- else if HasInputField .Input "userId" }}
		roomID = wsUserID
		{{- end }}

		initialRooms := map[string]struct{}{}
		if roomID != "" {
			initialRooms[roomID] = struct{}{}
		}
		client := &wsClient{
			hub:               {{ $.Name | ToLower }}Hub,
			conn:              conn,
			send:              make(chan []byte, 256),
			rooms:             initialRooms,
			allowDynamicRooms: {{ if .AllowDynamicRooms }}true{{ else }}false{{ end }},
			sessionID:         wsWatchedSessionID(r),
		}
		{{- if .IsLive }}
		// ?live=1 asks for the room as per-viewer snapshots; ?since=<seq> is
		// the last one the page applied (the missed ones are replayed). Without
		// JetStream the socket gets the plain event relay.
		if r.URL.Query().Get("live") == "1" && roomID != "" {
			if liveEP, liveRT := liveEndpointNamed("{{ .LiveName }}"), liveActive(); liveEP != nil && liveRT != nil {
				since, _ := strconv.ParseUint(r.URL.Query().Get("since"), 10, 64)
				client.live = &wsLiveClient{room: roomID, companyID: wsIdent.CompanyID}
				client.live.detach = func() { liveEP.detach(client) }
				if !liveEP.attach(liveRT, client, since) {
					conn.WriteMessage(websocket.CloseMessage, websocket.FormatCloseMessage(liveAccessRevoked, "access revoked"))
					conn.Close()
					return
				}
			}
		}
		{{- end }}
		client.hub.register <- client

		go client.writePump()
		go client.readPump()
	}
}
{{- end }}
