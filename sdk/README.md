# ANG Frontend SDK

This directory is generated by `ang build`.

## Purpose

- Typed API client
- Query helpers
- Generated forms/components
- Route metadata

## Usage

Install dependencies in this folder and run type-check:

```bash
npm install
npm run typecheck
```

Then import modules from `./sdk` in your frontend app.

