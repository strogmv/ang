// AUTO-GENERATED by ANG. DO NOT EDIT.
import { useEffect, useMemo, useState, type ReactNode } from 'react';
import { AutoForm } from '../ui/auto-form';
import type { FormSchema } from '../ui/auto-form';
import * as Hooks from '../../hooks';
import * as FormHooks from '../../forms';
import { resolveFormEntry, type GeneratedFormName } from './forms.manifest';

export type FormRendererProps = {
  formName: GeneratedFormName;
  onSuccess?: (data: unknown) => void;
  onError?: (error: Error) => void;
  onCancel?: () => void;
  defaultValues?: Record<string, unknown>;
  fallback?: ReactNode;
};

export function FormRenderer({ formName, onSuccess, onError, onCancel, defaultValues, fallback = null }: FormRendererProps) {
  const entry = useMemo(() => resolveFormEntry(formName), [formName]);
  const [schema, setSchema] = useState<FormSchema<Record<string, unknown>> | null>(null);
  const [loadErr, setLoadErr] = useState<Error | null>(null);
  useEffect(() => {
    let active = true;
    setSchema(null);
    setLoadErr(null);
    entry.loadSchema().then((next) => {
      if (!active) return;
      setSchema(next);
    }).catch((e) => {
      if (!active) return;
      setLoadErr(e as Error);
    });
    return () => { active = false; };
  }, [entry]);
  const formHookRegistry = FormHooks as Record<string, unknown>;
  const mutationHookRegistry = Hooks as Record<string, unknown>;
  const useForm = formHookRegistry[entry.useFormHook];
  const useMutation = mutationHookRegistry[entry.useMutationHook];
  if (typeof useForm !== 'function' || typeof useMutation !== 'function') {
    throw new Error(`ANG FormRenderer: hooks are not resolved for form ${formName}`);
  }
  if (loadErr) throw loadErr;
  if (!schema) return fallback;
  const form = (useForm as (options?: { defaultValues?: Record<string, unknown> }) => unknown)(defaultValues ? { defaultValues } : undefined);
  const { mutate, isPending } = (useMutation as (options?: { onSuccess?: (data: unknown) => void; onError?: (error: Error) => void }) => { mutate: (data: unknown) => void; isPending?: boolean })({
    onSuccess: (data: unknown) => onSuccess?.(data),
    onError: (error: Error) => onError?.(error),
  });
  return (
    <AutoForm
      form={form as import('react-hook-form').UseFormReturn<Record<string, unknown>>}
      schema={schema}
      onSubmit={(data) => mutate(data)}
      isPending={isPending}
      onCancel={onCancel}
    />
  );
}
