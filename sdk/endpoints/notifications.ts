// AUTO-GENERATED by ANG
import { apiClient, validateResponse, type ApiRequestOptions } from '../api-client';
import * as Types from '../types';

type ParamRecord = Record<string, unknown>;

const toParamRecord = (params: unknown): ParamRecord =>
  params && typeof params === 'object' ? (params as ParamRecord) : {};

const getPathParamValue = (params: ParamRecord, primary: string, aliases: string[] = []) => {
  const candidates = [primary, ...aliases];
  for (const key of candidates) {
    const value = params[key];
    if (value === undefined || value === null) {
      continue;
    }
    const text = String(value).trim();
    if (text !== '') {
      return text;
    }
  }
  throw new Error(`Missing required path param: ${primary}`);
};

const omitKeys = (params: ParamRecord, keys: string[]): ParamRecord => {
  const next: ParamRecord = { ...params };
  for (const key of keys) {
    delete next[key];
  }
  return next;
};
export const sendInvitationEmail = (
  params: Types.SendInvitationEmailRequest = {} as Types.SendInvitationEmailRequest,
  requestOptions: ApiRequestOptions = {}
) => {
  return apiClient.post<Types.SendInvitationEmailResponse>(`/notifications/invitation`, params, { signal: requestOptions.signal }).then((r) => {
    const data = r.data;
    if (import.meta.env.DEV) {
      validateResponse('SendInvitationEmailResponseSchema', data, 'SendInvitationEmail');
    }
    return data;
  });
};
export const sendPasswordResetEmail = (
  params: Types.SendPasswordResetEmailRequest = {} as Types.SendPasswordResetEmailRequest,
  requestOptions: ApiRequestOptions = {}
) => {
  return apiClient.post<Types.SendPasswordResetEmailResponse>(`/notifications/password-reset`, params, { signal: requestOptions.signal }).then((r) => {
    const data = r.data;
    if (import.meta.env.DEV) {
      validateResponse('SendPasswordResetEmailResponseSchema', data, 'SendPasswordResetEmail');
    }
    return data;
  });
};
