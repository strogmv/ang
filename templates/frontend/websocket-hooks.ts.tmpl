// Generated by ANG. Do not edit.
import { useQueryClient, type QueryClient } from '@tanstack/react-query';
import type * as Types from '../types';
import { WebSocketClient, type WebSocketClientOptions, type WSMessage } from '../websocket';
import * as endpoints from '../endpoints';

{{- $hasNotificationHotPatch := and (HasEvent "NotificationCreated") (HasGETRPC "ListNotifications") (HasEntity "ListNotificationsResponse") (HasEntity "ListNotificationsResponseData") }}
{{- $hasChatMessageHotPatch := and (HasEvent "ChatMessageReceived") (HasGETRPC "GetMessagesFromAChat") (HasEntity "GetMessagesFromAChatResponse") (HasEntity "GetMessagesFromAChatResponseData") }}
{{- $hasBidPlacedHotPatch := and (HasEvent "BidPlaced") (HasGETRPC "ListBids") (HasEntity "ListBidsResponse") (HasEntity "ListBidsResponseData") }}
{{- $hasHotEventCachePatcher := or $hasNotificationHotPatch $hasChatMessageHotPatch $hasBidPlacedHotPatch }}
import { useEffect, useRef, useLayoutEffect } from 'react';

/**
 * Per event: TanStack queryKey prefixes to invalidate (partial match).
 * Built from intent: frontend.ws.invalidateRPCs, invalidateServices, invalidateAll,
 * owner, consumers; or omitted → use defaultWsInvalidatePrefixes.
 */
export const wsInvalidatePrefixesByEventType: Partial<
  Record<WSMessage['type'], readonly (readonly string[])[]>
> = {
{{- range .WSInvalidateRules }}
{{- if and (not .Omit) (not .Noop) }}
  '{{ .EventName }}': [
  {{- range .Prefixes }}
    [{{- range $i, $p := . }}{{ if $i }}, {{ end }}'{{ $p }}'{{- end }}] as const,
  {{- end }}
  ],
{{- end }}
{{- end }}
};

/** Skip cache invalidation entirely for these WS event types (frontend.ws.noop). */
export const wsInvalidateNoopEventTypes = new Set<string>([
{{- range .WSInvalidateRules }}{{ if .Noop }}
  '{{ .EventName }}',
{{- end }}{{ end }}
]);

/** When an event type is not listed above: invalidate these service-level prefixes (all GET RPCs under each service). */
export const defaultWsInvalidatePrefixes: readonly (readonly string[])[] = [
{{- range GETServiceNames }}
  ['{{ . }}'] as const,
{{- end }}
];

const defaultWebSocketOptions: WebSocketClientOptions = { handleNetworkEvents: true };

{{- range .Endpoints }}
{{- if eq .Method "WS" }}
export type {{ .RPC }}WSMessage =
{{- if gt (len .Messages) 0 }}
  Extract<WSMessage, { type:
  {{- range $i, $msg := .Messages }}{{ if $i }} | {{ end }}'{{ $msg }}'{{- end }}
  }>;
{{- else }}
  WSMessage;
{{- end }}

{{- if eq (len (PathArgNames .Path)) 0 }}
export const {{ LowerFirst .RPC }}WsClient = new WebSocketClient<{{ .RPC }}WSMessage>(
  endpoints.{{ LowerFirst .RPC }}Url(),
  defaultWebSocketOptions
);

{{- if HasDynamicRoomSiblings . }}
export const subscribe{{ .RPC }}Room = (room: string) => {{ LowerFirst .RPC }}WsClient.subscribeRoom(room);
export const unsubscribe{{ .RPC }}Room = (room: string) => {{ LowerFirst .RPC }}WsClient.unsubscribeRoom(room);
export const set{{ .RPC }}Rooms = (rooms: readonly string[]) => {{ LowerFirst .RPC }}WsClient.setSubscribedRooms(rooms);

{{- end }}

{{- else }}
export const create{{ .RPC }}WsClient = (
  params: Types.{{ .RPC }}Request = {} as Types.{{ .RPC }}Request,
  token?: string | null,
  options: WebSocketClientOptions = {}
) => {
  const mergedOptions = { ...defaultWebSocketOptions, ...options };
  if (token === undefined || token === null) {
    return new WebSocketClient<{{ .RPC }}WSMessage>(endpoints.{{ LowerFirst .RPC }}Url(params), mergedOptions);
  }
  return new WebSocketClient<{{ .RPC }}WSMessage>(endpoints.{{ LowerFirst .RPC }}Url(params), token, mergedOptions);
};
{{- end }}
{{- end }}
{{- end }}

{{- if HasPrimaryAppWSEndpoint }}
export type AppWSMessage = {{ PrimaryAppWSRPC }}WSMessage;
export const appWsClient = {{ LowerFirst (PrimaryAppWSRPC) }}WsClient;

{{- end }}

export const defaultWsClients = [
{{- range .Endpoints }}
{{- if and (eq .Method "WS") (eq (len (PathArgNames .Path)) 0) }}
  {{- if HasPrimaryAppWSEndpoint }}
  {{- if IsPrimaryAppWSEndpoint . }}
  {{ LowerFirst .RPC }}WsClient,
  {{- end }}
  {{- else }}
  {{ LowerFirst .RPC }}WsClient,
  {{- end }}
{{- end }}
{{- end }}
] as const;

const isQueryKeyArray = (value: unknown): value is readonly unknown[] => Array.isArray(value);

const getQueryKeyParams = (queryKey: readonly unknown[]): Record<string, unknown> | null => {
  if (queryKey.length < 3) {
    return null;
  }
  const params = queryKey[2];
  if (!params || typeof params !== 'object' || Array.isArray(params)) {
    return null;
  }
  return params as Record<string, unknown>;
};

const matchesEndpointQuery = (queryKey: readonly unknown[], service: string, rpc: string) =>
  queryKey.length >= 2 && queryKey[0] === service && queryKey[1] === rpc;

const trimPatchedList = <T,>(items: readonly T[], maxSize: number) => {
  if (maxSize <= 0) {
    return items as T[];
  }
  return items.slice(0, maxSize);
};

const eventTimestamp = (payload: unknown) => {
  if (payload && typeof payload === 'object' && !Array.isArray(payload)) {
    const createdAt = (payload as { createdAt?: unknown }).createdAt;
    if (typeof createdAt === 'string' && createdAt.trim() !== '') {
      return createdAt;
    }
  }
  return new Date().toISOString();
};

{{- if $hasHotEventCachePatcher }}
const applyHotEventCacheUpdate = (queryClient: QueryClient, data: WSMessage): boolean => {
{{- if $hasNotificationHotPatch }}
  if (data.type === 'NotificationCreated') {
    let handled = false;
    queryClient.setQueriesData<Types.ListNotificationsResponse>(
      {
        predicate: (query) => isQueryKeyArray(query.queryKey) && matchesEndpointQuery(query.queryKey, 'Notifications', 'ListNotifications'),
      },
      (old) => {
        if (!old || !Array.isArray(old.data)) {
          return old;
        }
        handled = true;
        if (old.data.some((item) => item.id === data.payload.id)) {
          return old;
        }
        const nextItem: Types.ListNotificationsResponseData = { ...data.payload };
        return {
          ...old,
          data: [nextItem, ...old.data],
        };
      }
    );
    return handled;
  }
{{- end }}
{{- if $hasChatMessageHotPatch }}
  if (data.type === 'ChatMessageReceived') {
    let handled = false;
    queryClient.setQueriesData<Types.GetMessagesFromAChatResponse>(
      {
        predicate: (query) => {
          if (!isQueryKeyArray(query.queryKey) || !matchesEndpointQuery(query.queryKey, 'Chat', 'GetMessagesFromAChat')) {
            return false;
          }
          const params = getQueryKeyParams(query.queryKey);
          return params?.id === data.payload.chatId;
        },
      },
      (old) => {
        if (!old || !Array.isArray(old.data)) {
          return old;
        }
        handled = true;
        if (old.data.some((item) => item.id === data.payload.messageId)) {
          return old;
        }
        const nextItem: Types.GetMessagesFromAChatResponseData = {
          id: data.payload.messageId,
          chatId: data.payload.chatId,
          senderUserId: data.payload.senderUserId,
          senderCompanyId: data.payload.senderCompanyId,
          contactId: data.payload.senderUserId,
          value: data.payload.body,
          body: data.payload.body,
          attachmentId: data.payload.attachmentId || undefined,
          replyToId: data.payload.replyToId || undefined,
          createdAt: eventTimestamp(data.payload),
        };
        const currentSize = old.data.length;
        return {
          ...old,
          data: trimPatchedList([nextItem, ...old.data], currentSize),
          total: typeof old.total === 'number' ? old.total + 1 : old.total,
        };
      }
    );
    return handled;
  }
{{- end }}
{{- if $hasBidPlacedHotPatch }}
  if (data.type === 'BidPlaced') {
    let handled = false;
    queryClient.setQueriesData<Types.ListBidsResponse>(
      {
        predicate: (query) => {
          if (!isQueryKeyArray(query.queryKey) || !matchesEndpointQuery(query.queryKey, 'Bids', 'ListBids')) {
            return false;
          }
          const params = getQueryKeyParams(query.queryKey);
          return params?.tenderId === data.payload.tenderId;
        },
      },
      (old) => {
        if (!old || !Array.isArray(old.data)) {
          return old;
        }
        handled = true;
        if (old.data.some((item) => item.id === data.payload.bidId)) {
          return old;
        }
        const nextItem: Types.ListBidsResponseData = {
          id: data.payload.bidId,
          tenderId: data.payload.tenderId,
          companyId: data.payload.companyId,
          userId: data.payload.userId,
          amount: data.payload.amount,
          createdAt: eventTimestamp(data.payload),
        };
        const currentSize = old.data.length;
        return {
          ...old,
          data: trimPatchedList([nextItem, ...old.data], currentSize),
        };
      }
    );
    return handled;
  }
{{- end }}
  return false;
};
{{- else }}
const applyHotEventCacheUpdate = (_queryClient: QueryClient, _data: WSMessage): boolean => false;
{{- end }}

export const subscribeToWebSocketClient = <T extends WSMessage, K extends T['type']>(
  client: WebSocketClient<T>,
  topic: K,
  onMessage: (payload: Extract<T, { type: K }>['payload']) => void
) => client.subscribe((data: T) => {
  if (data.type === topic) {
    const matched = data as Extract<T, { type: K }>;
    const emit = onMessage as (payload: Extract<T, { type: K }>['payload']) => void;
    emit(matched.payload as never);
  }
});

/**
 * App-level helper for singleton sockets.
 * When /ws/app exists, this subscribes only to that canonical socket.
 * Otherwise it falls back to all generated singleton sockets.
 */
export const useWebsocketSubscription = <K extends WSMessage['type']>(
  topic: K,
  onMessage: (payload: Extract<WSMessage, { type: K }>['payload']) => void
) => {
  const onMessageRef = useRef(onMessage);
  useLayoutEffect(() => { onMessageRef.current = onMessage; });

  useEffect(() => {
    // Cast to WebSocketClient<WSMessage>: singleton clients carry all event types at runtime;
    // their static type is narrowed by what the WS endpoint declares, but
    // useWebsocketSubscription is a catch-all that must accept the full WSMessage union.
    const unsubs = (defaultWsClients as unknown as ReadonlyArray<WebSocketClient<WSMessage>>)
      .map((client) => subscribeToWebSocketClient(client, topic, (payload) => {
        const emit = onMessageRef.current as unknown as (payload: Extract<WSMessage, { type: K }>['payload']) => void;
        emit(payload as never);
      }));
    return () => {
      for (const unsub of unsubs) {
        unsub();
      }
    };
  }, [topic]);
};

{{- if HasPrimaryAppWSEndpoint }}
export const useAppWebsocketSubscription = <K extends AppWSMessage['type']>(
  topic: K,
  onMessage: (payload: Extract<AppWSMessage, { type: K }>['payload']) => void
) => {
  const onMessageRef = useRef(onMessage);
  useLayoutEffect(() => { onMessageRef.current = onMessage; });

  useEffect(() => {
    const unsubscribe = subscribeToWebSocketClient(appWsClient, topic, (payload) => {
      const emit = onMessageRef.current as unknown as (payload: Extract<AppWSMessage, { type: K }>['payload']) => void;
	  emit(payload as never);
    });
    return () => {
      unsubscribe();
    };
  }, [topic]);
};

{{- end }}

{{- range .Endpoints }}
{{- if and (eq .Method "WS") (gt (len (PathArgNames .Path)) 0) }}
/**
 * Endpoint-scoped subscription helper for {{ .RPC }}.
 */
export const use{{ .RPC }}Subscription = <K extends {{ .RPC }}WSMessage['type']>(
  params: Types.{{ .RPC }}Request,
  topic: K,
  onMessage: (payload: Extract<{{ .RPC }}WSMessage, { type: K }>['payload']) => void,
  token?: string | null,
  options: WebSocketClientOptions = {}
) => {
  const queryClient = useQueryClient();
  const onMessageRef = useRef(onMessage);
  useLayoutEffect(() => { onMessageRef.current = onMessage; });
  const socketUrl = endpoints.{{ LowerFirst .RPC }}Url(params);

  useEffect(() => {
    const client = token === undefined || token === null
      ? new WebSocketClient<{{ .RPC }}WSMessage>(socketUrl, { ...defaultWebSocketOptions, ...options })
      : new WebSocketClient<{{ .RPC }}WSMessage>(socketUrl, token, { ...defaultWebSocketOptions, ...options });
    const unsubscribe = client.subscribe((data: {{ .RPC }}WSMessage) => {
      applyHotEventCacheUpdate(queryClient, data as WSMessage);
      if (data.type === topic) {
        const matched = data as Extract<{{ .RPC }}WSMessage, { type: K }>;
        const emit = onMessageRef.current as unknown as (payload: Extract<{{ .RPC }}WSMessage, { type: K }>['payload']) => void;
		emit(matched.payload as never);
      }
    });
    return () => {
      unsubscribe();
      client.close();
    };
  }, [queryClient, socketUrl, topic, token]);
};

{{- end }}
{{- end }}

/**
 * Global hook to automatically invalidate queries based on app-level singleton WS events.
 * Call this once in your App root.
 */
export const useAutoInvalidation = () => {
  const queryClient = useQueryClient();

  useEffect(() => {
    const unsubs = defaultWsClients.map((client) => client.subscribe((data: WSMessage) => {
      const t = String(data.type);
      if (wsInvalidateNoopEventTypes.has(t)) {
        return;
      }
      if (applyHotEventCacheUpdate(queryClient, data)) {
        return;
      }
      const specific = wsInvalidatePrefixesByEventType[data.type];
      const prefixes = specific ?? defaultWsInvalidatePrefixes;
      for (const prefix of prefixes) {
        queryClient.invalidateQueries({ queryKey: prefix });
      }
    }));
    return () => {
      for (const unsub of unsubs) {
        unsub();
      }
    };
  }, [queryClient]);
};
