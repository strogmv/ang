"""Postgres adapter stub for {{ .RepoName }} generated by ANG."""

from __future__ import annotations

from typing import Any

from app.repositories.{{ .ModuleName }} import {{ .PortClassName }}


class {{ .PGClassName }}({{ .PortClassName }}):
{{- if .Finders }}
{{- $repo := .RepoName }}
{{- range .Finders }}
    async def {{ .Name }}(self, *args: Any, **kwargs: Any) -> Any:
        # ANG:BEGIN_CUSTOM {{ .CustomKey }}
        raise NotImplementedError("Implement postgres finder {{ $repo }}.{{ .Name }}")
        # ANG:END_CUSTOM {{ .CustomKey }}
{{- end }}
{{- else }}
    pass
{{- end }}
