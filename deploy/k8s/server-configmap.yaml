apiVersion: v1
kind: ConfigMap
metadata:
  name: server-config
data:
  ENV: "production"
  HTTP_PORT: "8080"
  # Daily scheduler clock times use this IANA timezone. Override per environment.
  SCHEDULER_TIMEZONE: "UTC"
  
  
  
  NATS_URL: "nats://nats-service:4222"
