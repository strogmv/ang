// Generated by ANG. Do not edit.
// MSW v2 request handlers — use in tests via setupServer(handlers)
import { http, HttpResponse, ws } from 'msw';
import type * as Types from '../types';

{{- range .Endpoints }}
{{- if eq .Method "WS" }}
const {{ .RPC | LowerFirst }}Link = ws.link('*/{{ RouterPath .Path }}');
{{- end }}
{{- end }}

// Override status codes for specific endpoints in tests:
// statusOverrides['GetUser'] = 404;
export const statusOverrides: Record<string, number> = {};

// Override response data for specific endpoints in tests:
// dataOverrides['GetUser'] = { id: 'test-id', email: 'test@test.com' };
export const dataOverrides: Record<string, unknown> = {};

{{- range .Endpoints }}
{{- if ne .Method "WS" }}
{{- $outName := printf "%sResponse" .RPC }}
{{- $entity := GetEntity $outName }}
{{- if not $entity }}
  {{- $baseName := .RPC | Replace "Get" "" | Replace "Edit" "" | Replace "Update" "" | Replace "Create" "" | Replace "List" "" | Replace "Delete" "" }}
  {{- $entity = GetEntity $baseName }}
  {{- if not $entity }}
    {{- $entity = GetEntity (printf "%sResponse" $baseName) }}
  {{- end }}
{{- end }}
/** Mock handler for {{ .Method }} {{ .Path }} */
export const mock{{ .RPC }} = http.{{ .Method | ToLower }}('*/{{ RouterPath .Path }}', () => {
  const status = statusOverrides['{{ .RPC }}'] ?? 200;
  if (status !== 200) {
    return HttpResponse.json(
      { code: 'MOCK_ERROR', title: 'Mocked error', detail: 'Status overridden in test', status },
      { status }
    );
  }
  const override = dataOverrides['{{ .RPC }}'];
  if (override !== undefined) {
    return HttpResponse.json(override);
  }
{{- if $entity }}
  const data: Partial<Types.{{ $entity.Name }}> = {
  {{- range $entity.Fields }}
    {{ .Name | LowerFirst }}: {{ MockValue . }},
  {{- end }}
  };
  return HttpResponse.json(data);
{{- else }}
  return HttpResponse.json({ message: 'Mocked response for {{ .RPC }}', timestamp: new Date().toISOString() });
{{- end }}
});
{{- end }}
{{- end }}

export const handlers = [
{{- range .Endpoints }}
{{- if eq .Method "WS" }}
  {{ .RPC | LowerFirst }}Link.addEventListener('connection', () => {}),
{{- end }}
{{- end }}
{{- range .Endpoints }}
{{- if ne .Method "WS" }}
  mock{{ .RPC }},
{{- end }}
{{- end }}
];
